---
phase: 05-renderer-ui-browse-search-edit-preview
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - electron/ui/lib/validation.ts
  - electron/ui/lib/filter.ts
  - electron/ui/lib/format.ts
  - test/ui/validation.test.ts
  - test/ui/filter.test.ts
  - test/ui/format.test.ts
autonomous: true
requirements: [EDIT-01, EDIT-02, EDIT-03, BROWSE-04, BROWSE-05]
must_haves:
  truths:
    - "The client validation mirror accepts/rejects at EXACTLY the patcher bounds; int64 stays a decimal string and is never coerced to a JS number."
    - "The filter predicate is a case-insensitive substring match with no RegExp built from user input."
    - "Level→XP echo reuses the golden-tested experience-table; int64 thousands-grouping is string-based and reversible."
  artifacts:
    - "electron/ui/lib/validation.ts: validateInt32, validateInt64, validateLevel, validateXp -> FieldResult"
    - "electron/ui/lib/filter.ts: matches(query, id)"
    - "electron/ui/lib/format.ts: levelToXpDisplay (reuses xpForLevel), groupInt64/ungroupInt64 (reversible string grouping)"
    - "test/ui/{validation,filter,format}.test.ts (Wave 0 scaffold, node:test + tsx --test)"
  key_links:
    - "format.ts is the ONLY renderer module that value-imports src/experience-table.ts (D-03, browser-safe pure Math)."
---

<objective>
Build the three pure, DOM-free client-logic modules and their Wave 0 unit tests (RESEARCH §Validation Architecture). These are the Nyquist-testable core of the renderer: the client validation mirror (D-04, bounds copied from src/patcher.ts), the filter predicate (BROWSE-04/05), and the format helpers (D-03 level→XP echo + int64 string grouping). All are unit-tested under the existing `tsx --test` runner with zero new test deps.

Purpose: Establish the instant as-you-type validation/filter/format behavior with automated proof before any component consumes it. The client mirror is UX-only; main's patcher stays authoritative.
Output: `validation.ts`, `filter.ts`, `format.ts` and their green test files.
</objective>

<execution_context>
@/home/jm/projects/mv2-save-editor/.claude/gsd-core/workflows/execute-plan.md
@/home/jm/projects/mv2-save-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-renderer-ui-browse-search-edit-preview/05-RESEARCH.md
@.planning/phases/05-renderer-ui-browse-search-edit-preview/05-UI-SPEC.md
@src/experience-table.ts
</context>

<artifacts_this_phase_produces>
- `electron/ui/lib/validation.ts`: `FieldResult`, `validateInt32`, `validateInt64`, `validateLevel(raw, levelCap)`, `validateXp` — bounds mirror src/patcher.ts.
- `electron/ui/lib/filter.ts`: `matches(query, id)` case-insensitive substring.
- `electron/ui/lib/format.ts`: level→XP display (reuses `xpForLevel`), reversible int64 string grouping.
- `test/ui/validation.test.ts`, `test/ui/filter.test.ts`, `test/ui/format.test.ts`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: validation.ts client mirror + tests (D-04, EDIT-01/02/03)</name>
  <files>electron/ui/lib/validation.ts, test/ui/validation.test.ts</files>
  <read_first>
    - src/patcher.ts lines 34–36 (INT32_MAX, INT64_MAX) and lines ~213–233 (`validatePlain` — the AUTHORITY whose bounds to mirror) plus the skill level `1..levelCap` and XP finite `>= 0` checks.
    - .planning/phases/05-renderer-ui-browse-search-edit-preview/05-UI-SPEC.md §"Copywriting Contract" (verbatim error strings: `Value must be between {min} and {max}.`, `Level must be between 1 and {levelCap}.`, `Enter a whole number.`).
    - .planning/phases/05-renderer-ui-browse-search-edit-preview/05-RESEARCH.md §"Code Examples" (validation mirror) + §"Pitfall 6" (bounds must match main exactly).
    - test/experience-table.test.ts lines 19–32 (test harness convention).
  </read_first>
  <behavior>
    - validateInt32: rejects non-integer / `< 0` / `> 2_147_483_647`; accepts 0 and 2_147_483_647; returns numeric value on success.
    - validateInt64: parses via BigInt (NEVER Number); rejects `< 0n` / `> 9_223_372_036_854_775_807n` and non-digit strings; on success returns the input as a decimal STRING (unchanged, no coercion) — a value above 2^53 round-trips exactly.
    - validateLevel(raw, levelCap): rejects non-integer / `< 1` / `> levelCap`; error copy embeds levelCap.
    - validateXp: rejects empty / non-finite / `< 0`; accepts a finite non-negative double.
  </behavior>
  <action>
    Create electron/ui/lib/validation.ts (DOM-free, no React import) exporting `FieldResult = { ok: true; value: string | number } | { ok: false; message: string }` and the four validators from RESEARCH §"Code Examples". Copy the constants `INT32_MAX = 2_147_483_647` and `INT64_MAX = 9_223_372_036_854_775_807n` to match src/patcher.ts EXACTLY (lower bound 0, not INT64_MIN). validateInt64 must use `BigInt(s)` for the range check and return the trimmed decimal string as `value` (D-06 — int64 never becomes a Number). Error messages must be the UI-SPEC Copywriting Contract strings verbatim, with `{min}`/`{max}`/`{levelCap}` interpolated. Carry the module header comment (owns: client range mirror; authority stays in main's patchSave; D-04).
    Create test/ui/validation.test.ts mirroring test/experience-table.test.ts (node:test describe/test + node:assert/strict). Assert: int32 boundary accept 0 and 2_147_483_647, reject -1 and 2_147_483_648 and "1.5"; int64 accepts a > 2^53 value and returns it as an unchanged string, rejects a value one past INT64_MAX and a non-digit; level rejects 0 / levelCap+1 with the levelCap-embedded message; xp rejects "" and -1, accepts a large finite double.
  </action>
  <verify>
    <automated>npx tsx --test 'test/ui/validation.test.ts'</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx --test 'test/ui/validation.test.ts'` passes.
    - `grep -q '9_223_372_036_854_775_807n' electron/ui/lib/validation.ts` and the module contains no `Number(` applied to the int64 raw input (int64 path uses `BigInt`).
    - Error strings match the UI-SPEC Copywriting Contract verbatim.
  </acceptance_criteria>
  <done>Client validation mirror matches patcher bounds; int64 stays a string; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: filter.ts predicate + format.ts (level→XP + int64 grouping) + tests</name>
  <files>electron/ui/lib/filter.ts, electron/ui/lib/format.ts, test/ui/filter.test.ts, test/ui/format.test.ts</files>
  <read_first>
    - src/experience-table.ts lines 63, 79 (exports `xpForLevel(level)`, `levelForXp(xp, levelCap)`).
    - .planning/phases/05-renderer-ui-browse-search-edit-preview/05-RESEARCH.md §"Code Examples" (filter predicate) + §"Anti-Patterns" (no new RegExp(query); no Number(gp)).
    - .planning/phases/05-renderer-ui-browse-search-edit-preview/05-PATTERNS.md §"filter.ts" and §"format.ts" (value-import rule: format.ts is the ONE renderer module that value-imports experience-table).
  </read_first>
  <behavior>
    - matches(query, id): empty query → true; otherwise `id.toLowerCase().includes(query.toLowerCase())`; never constructs a RegExp.
    - level→XP display: for level L, echoes `xpForLevel(L)` (e.g. 99 → 13_034_427) grouped for display.
    - int64 grouping is string-based and reversible: group(digits) inserts separators; ungroup(grouped) === original digit string, for a value well above 2^53 (no precision loss).
  </behavior>
  <action>
    Create electron/ui/lib/filter.ts exporting `matches(query: string, id: string): boolean` per RESEARCH §"Code Examples" — case-insensitive `String.prototype.includes`, empty query returns true, NEVER `new RegExp(userInput)` (ReDoS). Carry a header comment.
    Create electron/ui/lib/format.ts: value-import `import { xpForLevel } from '../../../src/experience-table'` (the ONLY permitted src/* value-import — D-03, pure Math, browser-safe). Export a `levelToXpDisplay(level: number): string` that returns the grouped `xpForLevel(level)`, and a reversible pair `groupInt64(digits: string): string` / `ungroupInt64(grouped: string): string` that operate purely on strings (insert/strip thousands separators) so `ungroupInt64(groupInt64(d)) === d` for any digit string — NEVER route the value through `Number` (D-06 precision). Carry a header comment.
    Create test/ui/filter.test.ts (case-insensitive substring: "log" matches "MelvorBase:NormalLog"; empty matches all; a regex-metachar query like "(" does not throw and matches literally). Create test/ui/format.test.ts (levelToXpDisplay(99) contains the digits of 13034427; groupInt64/ungroupInt64 round-trips a > 2^53 digit string unchanged).
  </action>
  <verify>
    <automated>npx tsx --test 'test/ui/filter.test.ts' 'test/ui/format.test.ts'</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx --test 'test/ui/filter.test.ts' 'test/ui/format.test.ts'` passes.
    - filter.ts contains no `RegExp`; format.ts value-imports `xpForLevel` from src/experience-table and its int64 grouping never calls `Number(` on the value.
    - format test proves `ungroupInt64(groupInt64(d)) === d` for a value above 2^53.
  </acceptance_criteria>
  <done>Filter predicate and format helpers implemented and unit-green; level→XP echo reuses the golden table; int64 grouping reversible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| user input → renderer | Filter query + edit text are untrusted renderer-side input. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-06 | DoS / Tampering | filter.ts | medium | mitigate | Plain `String.includes` on lowercased strings; never `new RegExp(userInput)` — no ReDoS/injection surface. |
| T-05-07 | Tampering (data integrity) | validation.ts / format.ts int64 | high | mitigate | int64 handled as decimal string via BigInt; grouping is string-only and reversible; never `Number()` — no precision loss past 2^53. Main re-validates authoritatively. |
</threat_model>

<verification>
- `npx tsx --test 'test/ui/validation.test.ts' 'test/ui/filter.test.ts' 'test/ui/format.test.ts'` green.
- No RegExp-from-input, no int64→Number coercion.
</verification>

<success_criteria>
Instant client validation/filter/format behavior is implemented as pure modules with automated proof; bounds mirror the patcher; int64 stays a string.
</success_criteria>

<output>
Create `.planning/phases/05-renderer-ui-browse-search-edit-preview/05-03-SUMMARY.md` when done.
</output>

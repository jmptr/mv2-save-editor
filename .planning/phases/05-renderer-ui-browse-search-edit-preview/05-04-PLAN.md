---
phase: 05-renderer-ui-browse-search-edit-preview
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - electron/ui/state/reducer.ts
  - electron/ui/state/selectors.ts
  - test/ui/reducer.test.ts
autonomous: true
requirements: [SAFE-02, EDIT-03]
must_haves:
  truths:
    - "The edit accumulator is keyed by fieldKey; an entry equal to its loaded ViewModel value contributes no edit (dirtiness is derived, not stored)."
    - "editsToPayload emits {fieldKey, newValue} with int64 as a decimal string; dirtyCount = editsToPayload length drives the Preview badge."
    - "Toggling a skill between Set-by-level and Set-XP clears the sibling key so level+xp are never both sent (no ConflictingEditError)."
  artifacts:
    - "electron/ui/state/reducer.ts: AppState, EditEntry, Action union, initialState, appReducer (pure)"
    - "electron/ui/state/selectors.ts: editsToPayload(state), dirtyCount(state)"
    - "test/ui/reducer.test.ts (Wave 0 scaffold)"
  key_links:
    - "appReducer SET_EDIT for a skill mode toggle also CLEAR_EDITs the sibling skill.<id>.{xp|level} key (Pitfall 5)."
---

<objective>
Build the single pure reducer + selectors that drive all renderer state (RESEARCH Pattern 2), plus the Wave 0 reducer test. This is the D-02 edit accumulator: a `Record<fieldKey, EditEntry>` where dirtiness is DERIVED (an entry equal to its loaded ViewModel value contributes no edit), `editsToPayload` produces the exact `{fieldKey, newValue}[]` for preview/write (int64 as string), and the reducer enforces the skill level/XP mutual exclusion (Pitfall 5) so main never returns `ConflictingEditError`.

Purpose: Centralize state transitions and the dirty→payload projection with automated proof before App wires them into React.
Output: `reducer.ts`, `selectors.ts`, `test/ui/reducer.test.ts`.
</objective>

<execution_context>
@/home/jm/projects/mv2-save-editor/.claude/gsd-core/workflows/execute-plan.md
@/home/jm/projects/mv2-save-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-renderer-ui-browse-search-edit-preview/05-RESEARCH.md
@.planning/phases/05-renderer-ui-browse-search-edit-preview/05-PATTERNS.md
@src/view-model.ts
@src/ipc/ipc-guards.ts
</context>

<artifacts_this_phase_produces>
- `electron/ui/state/reducer.ts`: `EditEntry`, `AppState`, `Action` union, `initialState`, pure `appReducer`.
- `electron/ui/state/selectors.ts`: `editsToPayload(state)`, `dirtyCount(state)`.
- `test/ui/reducer.test.ts`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: appReducer + AppState/Action + initialState (D-02, Pitfall 5)</name>
  <files>electron/ui/state/reducer.ts</files>
  <read_first>
    - .planning/phases/05-renderer-ui-browse-search-edit-preview/05-RESEARCH.md §"Pattern 2" (AppState/Action/EditEntry shapes; dirtiness derived) + §"Pitfall 5" (level+xp conflict) + §"Anti-Patterns" (storing both keys).
    - .planning/phases/05-renderer-ui-browse-search-edit-preview/05-PATTERNS.md §"reducer.ts" (import type discipline; ConflictingEditError context).
    - src/view-model.ts (`ViewModel` — import type only), src/ipc/ipc-guards.ts (`WireChangeRow`, `WireEdit` — import type only), src/patcher.ts (`Violation` — import type only).
  </read_first>
  <behavior>
    - LOAD_START → status 'loading'; LOAD_OK stores viewModel, status 'populated', clears edits/preview/write; LOAD_ERR sets status 'load-error' + kind/message; LOAD_CANCELLED returns to prior/empty without error.
    - SET_EDIT{fieldKey, entry} sets edits[fieldKey]; CLEAR_EDIT removes it. When fieldKey is `skill.<id>.xp`, SET_EDIT also removes `skill.<id>.level` (and vice-versa) so only one of the pair is ever present.
    - PREVIEW_OK stores {rows, violations}; PREVIEW_CLOSE clears preview. WRITE_START/OK/ERR/CANCELLED update the write sub-state; WRITE_OK stores outputPath.
  </behavior>
  <action>
    Create electron/ui/state/reducer.ts (DOM-free, no React import). All ViewModel/wire/Violation references via `import type` (esbuild-erased). Define `EditEntry { raw: string; value: string | number; valid: boolean }`, `AppState` (status union 'empty'|'loading'|'populated'|'load-error'; viewModel: ViewModel | null; edits: Record<string, EditEntry>; preview: { rows: WireChangeRow[]; violations: Violation[] } | null; write: { status: 'idle'|'writing'|'success'|'error'; outputPath?: string; message?: string }; optional errorKind/errorMessage), the `Action` discriminated union (LOAD_START, LOAD_OK, LOAD_CANCELLED, LOAD_ERR, SET_EDIT, CLEAR_EDIT, PREVIEW_OK, PREVIEW_CLOSE, WRITE_START, WRITE_OK, WRITE_CANCELLED, WRITE_ERR), `initialState`, and a PURE `appReducer(state, action)` returning a new state (no mutation). Implement the Pitfall 5 sibling-clear: in SET_EDIT, if the fieldKey matches `^skill\.(.+)\.(xp|level)$`, drop the sibling `skill.<id>.<other>` from the new edits map. Honor exactOptionalPropertyTypes (omit optional keys rather than assign undefined). Carry the module header comment.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` passes; reducer imports ViewModel/WireChangeRow/Violation via `import type` only.
    - appReducer is pure (returns new objects; does not mutate `state.edits`).
    - SET_EDIT of a `skill.<id>.xp` key removes any existing `skill.<id>.level` entry (and vice-versa).
  </acceptance_criteria>
  <done>Pure appReducer with the D-02 accumulator and Pitfall 5 mutual exclusion; typecheck green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: selectors (editsToPayload, dirtyCount) + reducer/selectors tests</name>
  <files>electron/ui/state/selectors.ts, test/ui/reducer.test.ts</files>
  <read_first>
    - src/ipc/ipc-guards.ts lines 147–154 (`toWireReport` — the pure map/project exemplar) + `WireEdit` shape.
    - .planning/phases/05-renderer-ui-browse-search-edit-preview/05-PATTERNS.md §"selectors.ts" (dirtiness derived; int64 as decimal string).
    - src/view-model.ts (`Summary.gp/slayerCoins` are strings; `BankItem.fieldKey`; `Skill.id`).
    - test/experience-table.test.ts lines 19–32 (harness).
  </read_first>
  <behavior>
    - editsToPayload(state): for each EditEntry that is `valid` AND whose value differs from the loaded ViewModel value for that fieldKey, emit `{ fieldKey, newValue: entry.value }`. int64 currency (`wallet.GoldPieces`/`wallet.SlayerCoins`) emits a decimal STRING; int32/double/level emit numbers.
    - An entry equal to its loaded value emits nothing (dirtiness derived). An invalid entry emits nothing.
    - dirtyCount(state) === editsToPayload(state).length.
  </behavior>
  <action>
    Create electron/ui/state/selectors.ts exporting `editsToPayload(state: AppState): WireEdit[]` and `dirtyCount(state: AppState): number`. To compare against the loaded value, resolve the loaded value by fieldKey against `state.viewModel`: `wallet.GoldPieces`→summary.gp (string), `wallet.SlayerCoins`→summary.slayerCoins (string), `bank.inventory...` → the bankItem with matching `fieldKey` (quantity number), `skill.<id>.xp`→skill.xp, `skill.<id>.level`→skill.level. Emit only valid, changed entries. int64 keys emit `entry.value` as-is (already a string from validation). `dirtyCount` delegates to `editsToPayload(...).length`.
    Create test/ui/reducer.test.ts (node:test + node:assert/strict, mirroring test/experience-table.test.ts). Build a minimal fake ViewModel (a couple of bankItems with distinct fieldKeys incl. a duplicate itemId, a skill with levelCap, gp/slayerCoins strings). Assert: (1) SET_EDIT equal-to-loaded contributes no edit and dirtyCount 0; (2) a changed valid int64 gp edit emits `{ fieldKey:'wallet.GoldPieces', newValue: '<string>' }` (string, not number) and dirtyCount 1; (3) an invalid entry emits nothing; (4) SET_EDIT skill xp then SET_EDIT the same skill's level leaves only the level key (Pitfall 5), editsToPayload has one skill edit; (5) two bankItems with the same itemId but distinct fieldKeys are independently editable.
  </action>
  <verify>
    <automated>npx tsx --test 'test/ui/reducer.test.ts'</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx --test 'test/ui/reducer.test.ts'` passes all five assertion groups.
    - editsToPayload emits int64 currency as a string (test asserts `typeof newValue === 'string'` for gp).
    - The Pitfall 5 test proves only one of skill xp/level survives in the payload.
  </acceptance_criteria>
  <done>Selectors derive the dirty payload (int64 as string) with automated proof of D-02, mutual exclusion, and dirtyCount.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| renderer accumulator → main (preview/write) | The edits payload is untrusted by main and re-validated. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-08 | Tampering (data integrity) | reducer / selectors | high | mitigate | Pitfall 5 mutual exclusion keeps level+xp from both crossing (avoids ConflictingEditError); int64 emitted as string; invalid/equal entries never emitted. Main re-validates authoritatively. |
| T-05-02 | Tampering | bank item addressing | medium | mitigate | editsToPayload resolves loaded values by the offset-free `BankItem.fieldKey` (Plan 05-01), so duplicate-itemId stacks are addressed distinctly. |
</threat_model>

<verification>
- `npx tsx --test 'test/ui/reducer.test.ts'` green; `npm run typecheck` green.
</verification>

<success_criteria>
The D-02 accumulator + derived payload + Pitfall 5 mutual exclusion are implemented as pure modules with automated proof; int64 stays a string end-to-end.
</success_criteria>

<output>
Create `.planning/phases/05-renderer-ui-browse-search-edit-preview/05-04-SUMMARY.md` when done.
</output>

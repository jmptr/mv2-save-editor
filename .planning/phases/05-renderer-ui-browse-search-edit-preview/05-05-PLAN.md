---
phase: 05-renderer-ui-browse-search-edit-preview
plan: 05
type: execute
wave: 2
depends_on: [05-02, 05-03]
files_modified:
  - electron/ui/styles.css
  - electron/ui/components/VirtualList.tsx
  - electron/ui/components/EditableCell.tsx
autonomous: true
requirements: [BROWSE-02, BROWSE-03, EDIT-02]
must_haves:
  truths:
    - "The shared virtualized list renders only windowed rows at a fixed 32px height and keys each row by a stable item id (not the virtual index)."
    - "The editable cell shows an inline validation error immediately for an invalid value and contributes no edit while invalid (D-04)."
    - "All static styling comes from one bundled stylesheet of CSS custom-property tokens per the UI-SPEC; dynamic row transform is inline CSSOM (CSP-safe)."
  artifacts:
    - "electron/ui/styles.css (:root tokens: --row-h 32px, spacing/type/color scales from UI-SPEC; layout, row/zebra/accent-bar, input/error, banner, modal classes)"
    - "electron/ui/components/VirtualList.tsx (generic @tanstack/react-virtual wrapper, fixed 32px, stable-id keys)"
    - "electron/ui/components/EditableCell.tsx (inline input + client-mirror validation + inline-error state)"
  key_links:
    - "EditableCell calls validation.ts and dispatches SET_EDIT on valid / shows the UI-SPEC inline error (contributes no edit) on invalid."
    - "VirtualList sets row transform via React inline style (CSSOM) so the strict CSP is satisfied; static styles live in styles.css."
---

<objective>
Build the shared UI primitives every panel reuses: the design-system stylesheet (UI-SPEC tokens), the `@tanstack/react-virtual` fixed-size list wrapper (RESEARCH Pattern 3, Pitfall 4) that backs BOTH side-by-side lists (D-05), and the inline `EditableCell` that runs the client validation mirror (D-04). These are the reusable building blocks consumed by the Summary/Bank/Skill panels in Wave 3.

Purpose: Centralize virtualization, inline-edit, and styling so panels stay thin and the UI-SPEC visual contract is applied in exactly one place.
Output: `styles.css`, `VirtualList.tsx`, `EditableCell.tsx`.
</objective>

<execution_context>
@/home/jm/projects/mv2-save-editor/.claude/gsd-core/workflows/execute-plan.md
@/home/jm/projects/mv2-save-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-renderer-ui-browse-search-edit-preview/05-UI-SPEC.md
@.planning/phases/05-renderer-ui-browse-search-edit-preview/05-RESEARCH.md
@.planning/phases/05-renderer-ui-browse-search-edit-preview/05-PATTERNS.md
@electron/ui/lib/validation.ts
</context>

<artifacts_this_phase_produces>
- `electron/ui/styles.css`: `:root` token block (spacing xs..3xl, 4 type roles/2 weights, color 60/30/10 + neutrals, `--row-h: 32px`, font stacks) and classes for layout, list rows (zebra alt-row, 2px accent left-bar for edited rows), inputs, inline error, focus ring, banner, modal.
- `electron/ui/components/VirtualList.tsx`: generic `VirtualList<T>` props `{ items, rowHeight?, renderRow, getKey }` using `useVirtualizer`.
- `electron/ui/components/EditableCell.tsx`: inline input running a passed validator, emitting valid value or showing inline error.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: styles.css token system + VirtualList wrapper</name>
  <files>electron/ui/styles.css, electron/ui/components/VirtualList.tsx</files>
  <read_first>
    - .planning/phases/05-renderer-ui-browse-search-edit-preview/05-UI-SPEC.md §"Design System", §"Spacing Scale" (--row-h 32px), §"Typography" (4 sizes/2 weights), §"Color" (60/30/10, accent reserved list, destructive note, focal point), §"Copywriting Contract".
    - .planning/phases/05-renderer-ui-browse-search-edit-preview/05-RESEARCH.md §"Pattern 3" (useVirtualizer fixed size) + §"Pitfall 4" (key by stable id, reset scroll on query change) + §"Pattern 1" (inline style CSSOM is CSP-safe).
    - .planning/phases/05-renderer-ui-browse-search-edit-preview/05-PATTERNS.md §"VirtualList.tsx" (no analog — follow Pattern 3 verbatim).
  </read_first>
  <action>
    Create electron/ui/styles.css: a `:root` block declaring the UI-SPEC tokens — spacing (`--space-xs:4px` .. `--space-3xl:64px`), the two font stacks (`--font-text`, `--font-mono`), the four type roles (summary 20/600, heading 16/600, body 14/400, label 12/600 uppercase 0.04em), the color tokens (`--c-dominant:#FFFFFF`, `--c-secondary:#F1F5F9`, `--c-accent:#2563EB`, `--c-destructive:#DC2626`, text `#0F172A`, muted `#64748B`, border `#E2E8F0`, hover `#F8FAFC`) and `--row-h:32px`. Add classes for: top layout (summary bar full width over the two side-by-side panels over the preview/write bar), panel + column headers, list container (`position:relative; overflow:auto`, fixed panel height), list row (height var(--row-h), zebra alt-row using `--c-secondary`, hover `#F8FAFC`), the 2px accent left-bar for an edited row (accent reserved-list #3), input (min-height 32px), inline error text (label 12/600 in `--c-destructive`) + invalid input border, focus ring (accent at 40% alpha — reserved #2), the pending-count badge (accent — reserved #4), the primary CTA fill (accent — reserved #1), banner, and modal/overlay. Only the accent reserved list may use accent; other interactive elements use neutral surfaces.
    Create electron/ui/components/VirtualList.tsx: a generic `VirtualList<T>` taking `{ items: T[]; getKey: (item: T) => string; renderRow: (item: T) => React.ReactNode; rowHeight?: number }`. Use `useVirtualizer({ count: items.length, getScrollElement: () => parentRef.current, estimateSize: () => rowHeight ?? 32, overscan: 8 })`. Render a scroll parent (ref) with an inner spacer sized to `getTotalSize()`, each virtual row `position:absolute; height:var(--row-h)` with `style={{ transform: \`translateY(${vi.start}px)\` }}` (inline CSSOM — CSP-safe) and `key={getKey(items[vi.index])}` (STABLE id, NOT vi.index — Pitfall 4). Carry the header comment.
  </action>
  <verify>
    <automated>npm run typecheck:ui && grep -q -- '--row-h' electron/ui/styles.css && grep -q 'useVirtualizer' electron/ui/components/VirtualList.tsx && grep -q 'getKey' electron/ui/components/VirtualList.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck:ui` passes.
    - styles.css declares `--row-h:32px` and the UI-SPEC color/spacing/type tokens; accent is only on the four reserved-list uses.
    - VirtualList keys rows via `getKey` (stable id), sets `estimateSize` to a constant, and uses inline-style transform.
  </acceptance_criteria>
  <done>Token stylesheet + fixed-size virtualization wrapper compile under the UI tsconfig; rows keyed by stable id.</done>
</task>

<task type="auto">
  <name>Task 2: EditableCell inline input + client-mirror validation (D-04)</name>
  <files>electron/ui/components/EditableCell.tsx</files>
  <read_first>
    - electron/ui/lib/validation.ts (`FieldResult` + the four validators).
    - .planning/phases/05-renderer-ui-browse-search-edit-preview/05-UI-SPEC.md §"Copywriting Contract" (inline error strings) + §"Data & Interaction Contract" (interaction states: field-editing, field-invalid contributes no edit).
    - .planning/phases/05-renderer-ui-browse-search-edit-preview/05-PATTERNS.md §"EditableCell.tsx" (local useState for in-progress text; valid → SET_EDIT, invalid → show error + CLEAR_EDIT; edited row gets accent left-bar).
  </read_first>
  <action>
    Create electron/ui/components/EditableCell.tsx: props `{ value: string | number; validate: (raw: string) => FieldResult; onValid: (value: string | number, raw: string) => void; onInvalid: () => void; mono?: boolean }`. Keep the in-progress text in local `useState` (do NOT route each keystroke through the top reducer — RESEARCH Pattern 2). On change, run `validate(raw)`: if `ok`, call `onValid(result.value, raw)` and clear any error; if not, set the inline-error message (UI-SPEC copy, from the validator) and call `onInvalid()` (invalid contributes no edit — D-04). Render the input with the `mono` class for numeric/int64 fields, the invalid-border + inline-error classes on error, and the focus ring. Do NOT use `dangerouslySetInnerHTML`. Carry the header comment.
  </action>
  <verify>
    <automated>npm run typecheck:ui && grep -q 'onInvalid' electron/ui/components/EditableCell.tsx && ! grep -q 'dangerouslySetInnerHTML' electron/ui/components/EditableCell.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck:ui` passes.
    - EditableCell calls the passed `validate`, invokes `onValid` only on `ok` and `onInvalid` otherwise, and renders the validator's UI-SPEC error copy inline.
    - No `dangerouslySetInnerHTML` anywhere in the file.
  </acceptance_criteria>
  <done>Inline editable cell runs the client mirror; invalid values show the UI-SPEC error and contribute no edit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| save-derived strings → DOM | itemId/name/gamemode originate from an untrusted .sav. |
| CSP boundary | Only same-origin styles/scripts; no runtime style injection. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-09 | Tampering / Info Disclosure (XSS) | EditableCell / VirtualList rows | high | mitigate | All save-derived strings render as React text children (auto-escaped); no `dangerouslySetInnerHTML`; strict CSP blocks injected script. |
| T-05-04 | Elevation | styles.css / CSP | medium | mitigate | Static styling is one same-origin stylesheet (no CSS-in-JS runtime injection); only the virtualizer transform uses inline CSSOM, which `style-src` does not govern. |
</threat_model>

<verification>
- `npm run typecheck:ui` green; token stylesheet + VirtualList + EditableCell present with the required behaviors.
</verification>

<success_criteria>
Reusable virtualization, inline-edit, and the UI-SPEC token stylesheet exist and typecheck under the scoped UI tsconfig, ready for the Wave 3 panels.
</success_criteria>

<output>
Create `.planning/phases/05-renderer-ui-browse-search-edit-preview/05-05-SUMMARY.md` when done.
</output>

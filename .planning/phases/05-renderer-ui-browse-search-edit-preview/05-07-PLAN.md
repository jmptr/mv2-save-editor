---
phase: 05-renderer-ui-browse-search-edit-preview
plan: 07
type: execute
wave: 3
depends_on: [05-02, 05-03, 05-04, 05-05]
files_modified:
  - electron/ui/components/SkillPanel.tsx
  - electron/ui/components/PreviewModal.tsx
  - electron/ui/components/Banner.tsx
autonomous: true
requirements: [BROWSE-03, BROWSE-05, EDIT-03, SAFE-02]
must_haves:
  truths:
    - "The skill panel lists all skills virtualized and filterable, with a per-skill mutually-exclusive Set-by-level / Set-XP mode; Set-by-level echoes the computed XP live before write."
    - "The preview modal renders main's authoritative old→new WireChangeRow rows plus any violations, and its CTA confirms the write."
    - "The banner maps each error kind to UI-SPEC copy (never a raw kind) and shows the non-blocking unknown-version warning."
  artifacts:
    - "electron/ui/components/SkillPanel.tsx (BROWSE-03/05, EDIT-03, D-03)"
    - "electron/ui/components/PreviewModal.tsx (SAFE-02, D-07/D-08)"
    - "electron/ui/components/Banner.tsx (error-kind→copy mapping + unknownVersion warning)"
  key_links:
    - "SkillPanel mode toggle dispatches SET_EDIT for exactly one of skill.<id>.level / skill.<id>.xp (reducer clears the sibling — Pitfall 5)."
    - "PreviewModal renders the changeReport from preview(); confirm CTA triggers write() (native Save-As); source of truth is main (D-07)."
---

<objective>
Build the right-column skill panel (BROWSE-03/05, EDIT-03, D-03), the pending-changes preview modal (SAFE-02, D-07/D-08), and the banner (error-kind→copy + non-blocking unknownVersion warning). The skill panel surfaces the mutually-exclusive Set-by-level / Set-XP edit modes with a live level→XP echo (display-only reuse of the golden table). The modal is the explicit in-app review gate that renders main's authoritative old→new rows before confirming the write.

Purpose: Complete the edit surfaces for skills and the preview/confirm gate SAFE-02 requires, mapping every result kind to safe copy.
Output: `SkillPanel.tsx`, `PreviewModal.tsx`, `Banner.tsx`.
</objective>

<execution_context>
@/home/jm/projects/mv2-save-editor/.claude/gsd-core/workflows/execute-plan.md
@/home/jm/projects/mv2-save-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-renderer-ui-browse-search-edit-preview/05-UI-SPEC.md
@.planning/phases/05-renderer-ui-browse-search-edit-preview/05-PATTERNS.md
@src/view-model.ts
@src/ipc/results.ts
@electron/ui/lib/validation.ts
@electron/ui/lib/format.ts
@electron/ui/lib/filter.ts
@electron/ui/components/VirtualList.tsx
@electron/ui/components/EditableCell.tsx
</context>

<artifacts_this_phase_produces>
- `electron/ui/components/SkillPanel.tsx`: filter + VirtualList over skills; per-skill Set-by-level/Set-XP toggle with live level→XP echo.
- `electron/ui/components/PreviewModal.tsx`: modal listing `WireChangeRow[]` (old→new) + violations; `Write New Save File` CTA.
- `electron/ui/components/Banner.tsx`: `ErrorKind`→UI-SPEC copy mapping + unknownVersion/unresolvedFields non-blocking notices.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: SkillPanel — filter/list + Set-by-level XOR Set-XP with live echo (EDIT-03, D-03)</name>
  <files>electron/ui/components/SkillPanel.tsx</files>
  <read_first>
    - src/view-model.ts lines 42–52 (`Skill` — id, xp, level, levelCap).
    - electron/ui/lib/format.ts (`levelToXpDisplay` — reuses xpForLevel), electron/ui/lib/validation.ts (`validateLevel`, `validateXp`), filter.ts, VirtualList.tsx, EditableCell.tsx.
    - .planning/phases/05-renderer-ui-browse-search-edit-preview/05-UI-SPEC.md §"Copywriting Contract" (`Set by level` · `Set XP`; filter empty copy swaps items→skills).
    - .planning/phases/05-renderer-ui-browse-search-edit-preview/05-PATTERNS.md §"SkillPanel.tsx" + §"Pitfall 5" (mode toggle clears sibling key; live echo display-only).
  </read_first>
  <action>
    Create electron/ui/components/SkillPanel.tsx: heading + as-you-type filter (local useState) over `skill.id` via `matches`, VirtualList over the filtered skills keyed by `skill.id`. Each row shows mono id, current xp and level (read-only display) and a per-skill mode toggle `Set by level` / `Set XP` (UI-SPEC copy). In `Set by level` mode: an EditableCell using `validateLevel(raw, skill.levelCap)`; on valid dispatch SET_EDIT with fieldKey `skill.<id>.level` (number) and show a live `levelToXpDisplay(level)` echo beside it (display-only — never a trust boundary, D-03). In `Set XP` mode: an EditableCell using `validateXp`; on valid dispatch SET_EDIT with fieldKey `skill.<id>.xp`. Switching modes for a skill dispatches CLEAR_EDIT for the sibling key (the reducer also enforces this — Pitfall 5) so level and xp are never both pending. Filter empty-result uses the UI-SPEC skills copy. Carry the header comment.
  </action>
  <verify>
    <automated>npm run typecheck:ui && grep -q 'levelToXpDisplay' electron/ui/components/SkillPanel.tsx && grep -q 'skill.' electron/ui/components/SkillPanel.tsx && grep -q 'Set by level' electron/ui/components/SkillPanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck:ui` passes.
    - SkillPanel dispatches SET_EDIT for exactly one of `skill.<id>.level` / `skill.<id>.xp` per skill and clears the sibling on mode switch.
    - Set-by-level shows a live `levelToXpDisplay` echo; filter uses the UI-SPEC skills copy.
  </acceptance_criteria>
  <done>Skill panel browses/filters (BROWSE-03/05) and edits level-or-xp (EDIT-03) with a live XP echo and mutual exclusion.</done>
</task>

<task type="auto">
  <name>Task 2: PreviewModal (SAFE-02, D-07/D-08) + Banner (kind→copy + warnings)</name>
  <files>electron/ui/components/PreviewModal.tsx, electron/ui/components/Banner.tsx</files>
  <read_first>
    - src/ipc/results.ts (`WireChangeRow`, `Violation`, `ErrorKind`, result types) and src/ipc/ipc-guards.ts lines 59–68 (`WireChangeRow` shape).
    - .planning/phases/05-renderer-ui-browse-search-edit-preview/05-UI-SPEC.md §"Copywriting Contract" (Write confirmation `Write {n} pending change(s)?`, `Write New Save File`, load/write error copy, write success `Saved to {outputPath}` with ✓, `No pending changes.`) + §"Color" (accent CTA fill; no green token; red only for problems) + §"Data & Interaction Contract" (states + kind list).
    - .planning/phases/05-renderer-ui-browse-search-edit-preview/05-PATTERNS.md §"PreviewModal.tsx" (renders main's changeReport, not a local diff — D-07) + §"Banner.tsx" (map 8 kinds; never surface raw kind; unknownVersion + unresolvedFields non-blocking).
  </read_first>
  <action>
    Create electron/ui/components/PreviewModal.tsx: props `{ rows: WireChangeRow[]; violations: Violation[]; count: number; onConfirm: () => void; onClose: () => void }`. Render a modal/overlay listing every `WireChangeRow` as mono `fieldKey`, `oldValue → newValue` (int64 rendered as its string, never reformatted through Number), plus any violations. Show the confirmation copy `Creates a new .sav next to the original — your original file is never changed. Write {n} pending change(s)?` and a primary CTA `Write New Save File` (accent fill) that calls `onConfirm` (which triggers `write(edits)` → native Save-As in App). The rendered rows are main's authoritative `preview()` output (D-07 — not a locally computed diff). Carry the header comment.
    Create electron/ui/components/Banner.tsx: a function mapping each `ErrorKind` to the UI-SPEC framed copy (load/parse error copy for load failures; `Write failed: {message}. Your original file was not changed.` for write-invariant/no-session/internal) — NEVER render a raw kind string. Also render the non-blocking `unknownVersion` warning (`Newer save version than tested — parsed anyway; review changes carefully.`) and, if present, an `unresolvedFields` ambiguity notice — both non-blocking (do not disable editing). Use `--c-destructive` for problem banners only; write-success uses neutral text + a ✓ glyph (no green token). Carry the header comment.
  </action>
  <verify>
    <automated>npm run typecheck:ui && grep -q 'Write New Save File' electron/ui/components/PreviewModal.tsx && grep -q 'pending change' electron/ui/components/PreviewModal.tsx && grep -q 'unknownVersion\|Newer save version' electron/ui/components/Banner.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck:ui` passes.
    - PreviewModal renders `WireChangeRow[]` old→new (int64 as string) + violations and a `Write New Save File` CTA calling onConfirm; copy matches the UI-SPEC.
    - Banner maps each `ErrorKind` to framed copy (no raw kind rendered) and shows the non-blocking unknown-version warning.
  </acceptance_criteria>
  <done>Preview/confirm modal (SAFE-02) renders main's authoritative rows; banner maps kinds to safe copy and warns non-blockingly on unknown version.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| main preview/write result → renderer | changeReport/violations/kind from main are rendered. |
| save-derived strings → DOM | skill ids, fieldKeys, messages. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-08 | Tampering (data integrity) | SkillPanel mode toggle | high | mitigate | Toggle dispatches exactly one of level/xp and clears the sibling; reducer enforces Pitfall 5 — no ConflictingEditError. |
| T-05-09 | Info Disclosure (XSS) | PreviewModal / Banner | high | mitigate | fieldKeys/messages/ids render as React text children (auto-escaped); no `dangerouslySetInnerHTML`; raw `kind` never surfaced (mapped to copy). |
| T-05-10 | Repudiation / safety | PreviewModal confirm gate | medium | mitigate | SAFE-02: write is gated behind an explicit modal showing main's authoritative old→new rows; no write occurs without confirm. |
</threat_model>

<verification>
- `npm run typecheck:ui` green; SkillPanel + PreviewModal + Banner present with required wiring/copy.
</verification>

<success_criteria>
Skill edit surfaces (EDIT-03, D-03), the SAFE-02 preview/confirm modal (D-07/D-08), and safe error/warning banners are implemented over the shared primitives and the frozen result contract.
</success_criteria>

<output>
Create `.planning/phases/05-renderer-ui-browse-search-edit-preview/05-07-SUMMARY.md` when done.
</output>

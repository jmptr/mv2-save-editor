---
phase: 05-renderer-ui-browse-search-edit-preview
plan: 08
type: execute
wave: 4
depends_on: [05-01, 05-02, 05-03, 05-04, 05-05, 05-06, 05-07]
files_modified:
  - electron/ui/App.tsx
  - electron/ui/AppContext.ts
  - electron/renderer.tsx
  - electron/renderer.ts
autonomous: false
requirements: [BROWSE-01, BROWSE-02, BROWSE-03, BROWSE-04, BROWSE-05, EDIT-01, EDIT-02, EDIT-03, SAFE-02]
must_haves:
  truths:
    - "The React app boots under the strict CSP: load → getModel → render summary + both panels → edit → preview → confirm → write, with loading/error/empty states."
    - "esbuild bundles the renderer to dist/renderer.js + dist/renderer.css with no Node builtins and no process-is-not-defined crash."
    - "An edited save written by the app loads in-game with the new values persisting after an in-game re-save (mandatory manual acceptance)."
  artifacts:
    - "electron/ui/App.tsx (useReducer orchestrator wiring SummaryBar/BankPanel/SkillPanel/PreviewModal/Banner)"
    - "electron/ui/AppContext.ts (dispatch context)"
    - "electron/renderer.tsx (createRoot mount; replaces renderer.ts which is deleted)"
  key_links:
    - "App maps each bridge result (load/getModel/preview/write) to a dispatch; Preview is enabled only when dirtyCount > 0; confirm calls write() → native Save-As."
    - "renderer.tsx imports styles.css so esbuild emits dist/renderer.css; index.html links it."
---

<objective>
Wire the whole renderer together and prove it end-to-end. `App` owns the single `useReducer`, lays out the two browse panels side by side (D-05), orchestrates the four bridge calls, maps each discriminated result to a dispatch, drives the pending-count badge from the local accumulator and gates Preview on `dirtyCount > 0` (D-02/D-07), opens the modal-backed authoritative preview from `preview()` (D-07), and triggers `write()` (native Save-As) from the modal confirm (D-08). `renderer.tsx` mounts it with `createRoot` and imports the stylesheet. Then the full esbuild build runs and the mandatory manual in-game acceptance (ROADMAP §Phase 5 criterion 5) is performed.

Purpose: Deliver the runnable app and close the phase with the one verification that cannot be automated — the edited save loading in-game.
Output: `App.tsx`, `AppContext.ts`, `renderer.tsx` (replacing `renderer.ts`), a green build, and human sign-off.
</objective>

<execution_context>
@/home/jm/projects/mv2-save-editor/.claude/gsd-core/workflows/execute-plan.md
@/home/jm/projects/mv2-save-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-renderer-ui-browse-search-edit-preview/05-RESEARCH.md
@.planning/phases/05-renderer-ui-browse-search-edit-preview/05-UI-SPEC.md
@.planning/phases/05-renderer-ui-browse-search-edit-preview/05-PATTERNS.md
@electron/renderer.ts
@src/ipc/results.ts
@electron/ui/state/reducer.ts
@electron/ui/state/selectors.ts
@electron/ui/components/SummaryBar.tsx
@electron/ui/components/BankPanel.tsx
@electron/ui/components/SkillPanel.tsx
@electron/ui/components/PreviewModal.tsx
@electron/ui/components/Banner.tsx
</context>

<artifacts_this_phase_produces>
- `electron/ui/App.tsx`: top-level `useReducer(appReducer, initialState)`; bridge orchestration; layout (summary bar over two panels over preview/write bar); Preview/Write controls; modal + banner wiring.
- `electron/ui/AppContext.ts`: React context carrying `dispatch` (RESEARCH Pattern 2).
- `electron/renderer.tsx`: `createRoot(#root).render(<App/>)` + `import './ui/styles.css'` (renderer.tsx sits at `electron/`, the stylesheet at `electron/ui/styles.css`).
- Deleted `electron/renderer.ts` (throwaway smoke test).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: App orchestrator + AppContext (bridge → dispatch, preview/confirm/write)</name>
  <files>electron/ui/App.tsx, electron/ui/AppContext.ts</files>
  <read_first>
    - electron/renderer.ts lines 22–41 (the imperative bridge-call orchestration being generalized) and its no-Node/offset boundary comment.
    - electron/ui/state/reducer.ts (AppState/Action/initialState/appReducer) + selectors.ts (editsToPayload, dirtyCount).
    - src/ipc/results.ts (result unions + `window.saveEditor`), each component's props (SummaryBar/BankPanel/SkillPanel/PreviewModal/Banner).
    - .planning/phases/05-renderer-ui-browse-search-edit-preview/05-UI-SPEC.md §"Copywriting Contract" (`Open Save File…`, `Preview Changes`, `No pending changes.`, success/error copy) + §"Data & Interaction Contract" (loading disables CTAs; interaction states) + §"Color" (pending-count badge; CTA).
    - .planning/phases/05-renderer-ui-browse-search-edit-preview/05-RESEARCH.md §"Pattern 2" (single reducer + AppContext; wrap row handlers in useCallback).
  </read_first>
  <action>
    Create electron/ui/AppContext.ts exporting a React context that carries `dispatch` (and, if convenient, the current `edits`), per RESEARCH Pattern 2 (no external store). Create electron/ui/App.tsx: `const [state, dispatch] = useReducer(appReducer, initialState)`. Wire the four bridge methods (`window.saveEditor.load/getModel/preview/write`), each in an async handler that dispatches the matching action based on the discriminated result (LOAD_START→result→LOAD_OK/LOAD_CANCELLED/LOAD_ERR; on LOAD_OK immediately call getModel() and store the viewModel; PREVIEW→PREVIEW_OK{rows,violations}; WRITE_START→WRITE_OK{outputPath}/WRITE_CANCELLED/WRITE_ERR). Compute `const payload = editsToPayload(state)` and `const count = dirtyCount(state)`; the `Preview Changes` control is disabled when `count === 0` (showing `No pending changes.`), and opening it calls `preview(payload)`. The modal's confirm calls `write(payload)`. Render the empty state (`No save loaded` + `Open Save File…`), loading state (disable CTAs), and the populated layout: `<Banner>` (unknownVersion/errors) over `<SummaryBar>` (full width) over `<BankPanel>` | `<SkillPanel>` over the preview/write bar with the pending-count badge, then `<PreviewModal>` when `state.preview` is non-null. Wrap row-level handlers passed to panels in `useCallback`. Do not touch Node/fs/offsets — only `window.saveEditor.*`. Carry the header comment.
  </action>
  <verify>
    <automated>npm run typecheck:ui && grep -q 'useReducer' electron/ui/App.tsx && grep -q 'editsToPayload' electron/ui/App.tsx && grep -q 'window.saveEditor' electron/ui/App.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck:ui` passes with App wiring all five components.
    - App maps each bridge result to a dispatch; Preview is disabled at `dirtyCount === 0`; confirm calls `write(payload)`.
    - App references only `window.saveEditor.*` (no `node:`/`fs`/`electron`/offset access).
  </acceptance_criteria>
  <done>App orchestrates the full load→edit→preview→confirm→write loop over the reducer and bridge.</done>
</task>

<task type="auto">
  <name>Task 2: renderer.tsx mount, delete renderer.ts, full build + typecheck + suite</name>
  <files>electron/renderer.tsx, electron/renderer.ts</files>
  <read_first>
    - electron/renderer.ts (the file being replaced — copy its header-comment boundary convention).
    - .planning/phases/05-renderer-ui-browse-search-edit-preview/05-RESEARCH.md §"Code Examples" (Root mount) + §"Runtime State Inventory" (delete stale renderer.ts) + §"Pitfall 1/2/3" (entry name; iife; type-only imports).
    - scripts/build.mjs (renderer entry now `electron/renderer.tsx`, from Plan 05-02).
  </read_first>
  <action>
    Create electron/renderer.tsx: `import { createRoot } from 'react-dom/client'`, `import { App } from './ui/App'`, `import './ui/styles.css'` (renderer.tsx lives at `electron/renderer.tsx`; the stylesheet is `electron/ui/styles.css`, so the relative path is `./ui/styles.css` — NOT `./styles.css`, which would fail esbuild resolution and drop `dist/renderer.css`); get `#root` and `createRoot(root).render(<App />)` guarded on a non-null root (React 19 createRoot, NOT ReactDOM.render). Keep the no-Node/offset header comment. Delete electron/renderer.ts (the throwaway). Run the full build and checks: `npm run build:electron` must emit `dist/renderer.js` and `dist/renderer.css` with no esbuild warning about a node builtin in the browser bundle; `npm run typecheck` and `npm run typecheck:ui` and `npm test` must all pass.
  </action>
  <verify>
    <automated>npm run build:electron && test -f dist/renderer.js && test -f dist/renderer.css && ! grep -RqiE "node:(fs|zlib)" dist/renderer.js && npm run typecheck && npm run typecheck:ui && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build:electron` emits `dist/renderer.js` + `dist/renderer.css`; the renderer bundle references no `node:fs`/`node:zlib`.
    - `electron/renderer.ts` is deleted; `electron/renderer.tsx` mounts `<App/>` via `createRoot`.
    - `npm run typecheck`, `npm run typecheck:ui`, and `npm test` (full suite) all pass.
  </acceptance_criteria>
  <done>The app builds to a clean browser bundle and all automated gates are green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual in-game acceptance (ROADMAP §Phase 5 criterion 5 — manual-only)</name>
  <action>Human runs the full renderer loop against a real save and confirms the app-written .sav loads in-game. This behavior cannot be automated (the game must load the file); it is recorded here as the mandatory manual acceptance.</action>
  <what-built>The full renderer loop, built to dist/: launch with `npm start`, load a real .sav, read the summary, browse/filter both lists, edit GP/Slayer Coins + a bank quantity + a skill (Set-by-level and Set-XP), preview old→new, confirm, and write a new .sav via the native Save-As dialog. This is ROADMAP §Phase 5 success criterion 5 — the one acceptance that cannot be automated (the game must load the file).</what-built>
  <how-to-verify>
    1. Run `npm start` (WSL2 needs libnss3/libnspr4/libasound2t64 + the GTK stack per 04-05).
    2. `Open Save File…` and load a real Melvor Idle 2 `.sav`; confirm the summary shows name, gamemode, total level, and mono GP/Slayer Coins (unchanged large int64 values, not rounded).
    3. Filter the bank list by an item ID and the skill list by a skill ID; confirm as-you-type narrowing and the `No matches` empty state.
    4. Edit GP (click-to-edit), a bank item quantity, and one skill via `Set by level` (watch the live XP echo) and another via `Set XP`; confirm invalid values show the inline error and do not increment the pending badge.
    5. `Preview Changes`; confirm the modal lists each field old→new; click `Write New Save File`; choose an output path in the native dialog; confirm `Saved to {outputPath}` and that the ORIGINAL file is unchanged.
    6. MANDATORY: load the written `.sav` in Melvor Idle 2; confirm the edited GP/quantity/skill values are present, then re-save in-game and reload to confirm they persist.
  </how-to-verify>
  <resume-signal>Type "approved" once the edited save loads in-game with the new values persisting, or describe the discrepancy.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| renderer → main (four channels) | App calls only the frozen bridge; main re-validates. |
| CSP boundary | The built bundle runs under strict CSP with no remote/eval. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03 | Info Disclosure | renderer bundle | high | mitigate | Type-only imports (`import type`) keep src/* runtime out of dist/renderer.js; build gate greps for node builtins; only experience-table value-import allowed. |
| T-05-10 | Repudiation / safety | write confirm gate | high | mitigate | write() only fires from the modal confirm (SAFE-02); main writes a NEW file (non-destructive); manual in-game acceptance is the final gate. |
| T-05-04 | Elevation | CSP at runtime | high | mitigate | iife bundle + external same-origin CSS; CSP meta unchanged; no remote URLs; navigation/window-open denied by Phase 4 controls. |
</threat_model>

<verification>
- `npm run build:electron` clean (dist/renderer.js + renderer.css, no node builtins); `npm run typecheck`, `typecheck:ui`, and `npm test` green.
- Human-verified: edited save loads in-game and values persist after an in-game re-save (manual-only, ROADMAP criterion 5).
</verification>

<success_criteria>
The renderer loop runs end-to-end under strict CSP and an app-written `.sav` loads in-game with the edited values persisting.
</success_criteria>

<output>
Create `.planning/phases/05-renderer-ui-browse-search-edit-preview/05-08-SUMMARY.md` when done.
</output>

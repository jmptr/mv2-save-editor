---
phase: 05-renderer-ui-browse-search-edit-preview
plan: 06
type: execute
wave: 3
depends_on: [05-01, 05-02, 05-03, 05-04, 05-05]
files_modified:
  - electron/ui/components/SummaryBar.tsx
  - electron/ui/components/BankPanel.tsx
autonomous: true
requirements: [BROWSE-01, BROWSE-02, BROWSE-04, EDIT-01, EDIT-02]
must_haves:
  truths:
    - "The summary bar shows read-only name, gamemode, total level, and mono int64 GP/Slayer Coins never coerced to a Number."
    - "GP and Slayer Coins are click-to-edit inline int64 fields feeding the accumulator as decimal strings; name/gamemode/total level stay read-only."
    - "The bank panel lists all stacks virtualized, filters as-you-type by itemId, and edits each stack's quantity inline addressed by BankItem.fieldKey."
  artifacts:
    - "electron/ui/components/SummaryBar.tsx (BROWSE-01, EDIT-01/D-06)"
    - "electron/ui/components/BankPanel.tsx (BROWSE-02/04, EDIT-02)"
  key_links:
    - "SummaryBar GP/SC edits dispatch SET_EDIT with fieldKey wallet.GoldPieces / wallet.SlayerCoins (int64 decimal string)."
    - "BankPanel quantity edits dispatch SET_EDIT with the offset-free BankItem.fieldKey."
---

<objective>
Build the read-only summary bar with click-to-edit int64 currencies (BROWSE-01, EDIT-01, D-06) and the bank browse+edit panel (BROWSE-02/04, EDIT-02). These are the left-column projection surfaces: the summary anchors the view; the bank panel virtualizes ~689 stacks, filters by itemId, and edits quantities inline addressed by the offset-free `BankItem.fieldKey` from Plan 05-01.

Purpose: Surface the summary and the bank list/edit loop over the shared primitives, keeping int64 a string end-to-end.
Output: `SummaryBar.tsx`, `BankPanel.tsx`.
</objective>

<execution_context>
@/home/jm/projects/mv2-save-editor/.claude/gsd-core/workflows/execute-plan.md
@/home/jm/projects/mv2-save-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-renderer-ui-browse-search-edit-preview/05-UI-SPEC.md
@.planning/phases/05-renderer-ui-browse-search-edit-preview/05-PATTERNS.md
@src/view-model.ts
@electron/ui/lib/validation.ts
@electron/ui/lib/filter.ts
@electron/ui/components/VirtualList.tsx
@electron/ui/components/EditableCell.tsx
</context>

<artifacts_this_phase_produces>
- `electron/ui/components/SummaryBar.tsx`: renders `Summary`; click-to-edit GP/SC via EditableCell + validateInt64.
- `electron/ui/components/BankPanel.tsx`: filter box + VirtualList over filtered `bankItems`; per-row quantity EditableCell + validateInt32.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: SummaryBar — read-only summary + click-to-edit int64 GP/SC (D-06)</name>
  <files>electron/ui/components/SummaryBar.tsx</files>
  <read_first>
    - src/view-model.ts lines 60–71 (`Summary` — name, gamemode, totalLevel, gp:string, slayerCoins:string).
    - .planning/phases/05-renderer-ui-browse-search-edit-preview/05-UI-SPEC.md §"Typography" (summary value mono 20/600), §"Color" (focal point; accent left-bar on edited), §"Copywriting Contract".
    - electron/ui/lib/validation.ts (`validateInt64`), electron/ui/components/EditableCell.tsx.
    - .planning/phases/05-renderer-ui-browse-search-edit-preview/05-PATTERNS.md §"SummaryBar.tsx" (int64 never Number; click-to-edit → SET_EDIT decimal string; name/gamemode/totalLevel read-only).
  </read_first>
  <action>
    Create electron/ui/components/SummaryBar.tsx: props `{ summary: Summary; edits; dispatch }` (accept the accumulator + dispatch, or narrower callbacks). Render name, gamemode, and totalLevel strictly read-only. Render `gp` and `slayerCoins` as mono 20/600 strings NEVER passed through `Number` (D-06). Each currency renders read-only with a ✎ affordance (Unicode glyph or inline SVG — no icon dep) that turns it into an inline EditableCell using `validateInt64`; on valid, dispatch SET_EDIT with fieldKey `wallet.GoldPieces` / `wallet.SlayerCoins` and the decimal-string value; an edited currency shows the accent left-bar. All strings render as React text children (auto-escaped). Carry the header comment.
  </action>
  <verify>
    <automated>npm run typecheck:ui && grep -q 'wallet.GoldPieces' electron/ui/components/SummaryBar.tsx && grep -q 'wallet.SlayerCoins' electron/ui/components/SummaryBar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck:ui` passes.
    - SummaryBar dispatches SET_EDIT for `wallet.GoldPieces` and `wallet.SlayerCoins` via validateInt64; the int64 value is never passed through `Number`.
    - name/gamemode/totalLevel are read-only (no edit affordance).
  </acceptance_criteria>
  <done>Summary bar renders BROWSE-01 read-only fields and click-to-edit int64 currencies (EDIT-01, D-06) as strings.</done>
</task>

<task type="auto">
  <name>Task 2: BankPanel — filter + virtualized list + inline quantity edit (BROWSE-02/04, EDIT-02)</name>
  <files>electron/ui/components/BankPanel.tsx</files>
  <read_first>
    - src/view-model.ts lines 25–36 (`BankItem` — now incl. `fieldKey` from Plan 05-01) and the isPlaceholder/isLocked metadata.
    - electron/ui/lib/filter.ts (`matches`), electron/ui/components/VirtualList.tsx, electron/ui/components/EditableCell.tsx, electron/ui/lib/validation.ts (`validateInt32`).
    - .planning/phases/05-renderer-ui-browse-search-edit-preview/05-UI-SPEC.md §"Copywriting Contract" (filter empty-result copy) + §"Data & Interaction Contract" (filtered-empty state).
    - .planning/phases/05-renderer-ui-browse-search-edit-preview/05-PATTERNS.md §"BankPanel.tsx" (filter text local useState; useMemo filtered; VirtualList keyed by fieldKey; Pitfall 4 reset scroll).
  </read_first>
  <action>
    Create electron/ui/components/BankPanel.tsx: a panel with a heading, an as-you-type filter input kept in LOCAL useState (do not route through the top reducer), `const filtered = useMemo(() => items.filter(i => matches(q, i.itemId)), [items, q])`, and a VirtualList over `filtered` with `getKey={(i) => i.fieldKey}` (stable offset-free key — Pitfall 4). Each row renders the mono itemId, isPlaceholder/isLocked metadata, and an EditableCell for quantity using `validateInt32`; on valid dispatch SET_EDIT with the item's `fieldKey`, on invalid dispatch CLEAR_EDIT for that key. When `filtered.length === 0` and `q !== ''` render the UI-SPEC filter empty-result copy (`No matches` / `No items match "{query}". ...`). Reset the list scroll to top on query change. All save-derived strings render as escaped React text children. Carry the header comment.
  </action>
  <verify>
    <automated>npm run typecheck:ui && grep -q 'useMemo' electron/ui/components/BankPanel.tsx && grep -q 'fieldKey' electron/ui/components/BankPanel.tsx && grep -q 'validateInt32' electron/ui/components/BankPanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck:ui` passes.
    - BankPanel virtualizes filtered items keyed by `BankItem.fieldKey`, filters via `matches`, and edits quantity via `validateInt32` dispatching SET_EDIT with the item's `fieldKey`.
    - Filter empty-result renders the UI-SPEC `No matches` copy.
  </acceptance_criteria>
  <done>Bank panel browses (BROWSE-02), filters as-you-type (BROWSE-04), and edits quantities inline (EDIT-02) addressed by offset-free fieldKey.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| save-derived strings → DOM | itemId / summary fields from an untrusted .sav. |
| user input → renderer | filter query + quantity/currency edits. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-09 | Tampering / Info Disclosure (XSS) | SummaryBar / BankPanel rows | high | mitigate | itemId/name/gamemode render as React text children (auto-escaped); no `dangerouslySetInnerHTML`; strict CSP. |
| T-05-07 | Tampering (data integrity) | int64 GP/SC edit | high | mitigate | validateInt64 keeps GP/SC a decimal string (never Number); SET_EDIT carries the string; main re-validates. |
| T-05-06 | DoS | filter query | low | mitigate | `matches` uses String.includes (Plan 05-03), no RegExp from input. |
</threat_model>

<verification>
- `npm run typecheck:ui` green; SummaryBar + BankPanel present with the required wiring.
</verification>

<success_criteria>
The read-only summary (BROWSE-01), click-to-edit int64 currencies (EDIT-01), and the filterable virtualized bank list with inline quantity edits (BROWSE-02/04, EDIT-02) are implemented over the shared primitives.
</success_criteria>

<output>
Create `.planning/phases/05-renderer-ui-browse-search-edit-preview/05-06-SUMMARY.md` when done.
</output>

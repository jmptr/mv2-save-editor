---
phase: 04-electron-shell-secure-ipc-non-destructive-write
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
autonomous: false
requirements: [IO-02]
user_setup: []

must_haves:
  truths:
    - "electron@43.0.0 and esbuild are present as devDependencies and resolvable by the build/run toolchain"
    - "The esbuild-postinstall (least-privilege) decision is made consciously by the developer before any script runs"
  artifacts:
    - "package.json devDependencies include electron@43.0.0 and esbuild@^0.28.1 (or the tsc-fallback recorded if declined)"
    - "package-lock.json updated to match"
  key_links:
    - "electron resolvable -> Plan 04/05 can compile+launch the shell"
    - "esbuild resolvable (or tsc fallback chosen) -> Plan 05 build:electron pipeline"
---

<objective>
Install the two new devDependencies this phase needs — `electron@43.0.0` (the desktop shell) and
`esbuild@^0.28.1` (the main/preload/renderer build) — behind a blocking human checkpoint that
resolves the one genuine supply-chain decision in the phase: esbuild ships a `postinstall`
(`node install.js`) that downloads its platform binary, and STATE.md records that Plan 01-01
DELIBERATELY declined the esbuild postinstall ("least-privilege supply-chain stance"). This plan
does not silently re-introduce that script — the developer explicitly chooses (a) allow the
postinstall for the real build step now, or (b) keep the least-privilege stance and adopt the
`tsc --outDir` no-postinstall fallback.

Purpose: unblock the Electron shell (Plans 04-04, 04-05) without violating the recorded
least-privilege decision. Foundational to IO-02 — no shell means no non-destructive write path.
Output: updated `package.json` / `package-lock.json`; a recorded install decision.
</objective>

<execution_context>
@/home/jm/projects/mv2-save-editor/.claude/gsd-core/workflows/execute-plan.md
@/home/jm/projects/mv2-save-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-electron-shell-secure-ipc-non-destructive-write/04-RESEARCH.md
@.planning/phases/04-electron-shell-secure-ipc-non-destructive-write/04-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Supply-chain checkpoint — verify legitimacy + decide esbuild postinstall</name>
  <files>package.json</files>
  <read_first>
    - .planning/phases/04-electron-shell-secure-ipc-non-destructive-write/04-RESEARCH.md (§Package Legitimacy Audit; §Pitfall 5; §Open Question 1)
    - .planning/STATE.md (Plan 01-01 note: esbuild postinstall deliberately NOT approved — least-privilege stance)
  </read_first>
  <action>
    Halt for a blocking human decision BEFORE any install runs. Present the legitimacy evidence for
    electron@43.0.0 and esbuild@^0.28.1 (both SUS `too-new` false-positives with official repos +
    CLAUDE.md endorsement) and the esbuild-postinstall vs least-privilege tradeoff. Do not run
    `npm install` until the developer replies with option-a (allow postinstall) or option-b (tsc
    fallback). This gate is not auto-approvable.
  </action>
  <what-built>
    Nothing installed yet. About to add two devDependencies both flagged SUS (`too-new`) by the
    legitimacy audit but each canonical, huge-download, official-repo, and named in CLAUDE.md's
    blessed stack:
      - electron@43.0.0 — no postinstall in metadata; SUS flag is release-cadence only.
      - esbuild@^0.28.1 — HAS `postinstall: node install.js` (downloads the @esbuild/<platform>
        binary). STATE.md records Plan 01-01 chose NOT to auto-approve this exact script.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/electron and confirm publisher/repo (github.com/electron/electron), version 43.0.0.
    2. Open https://www.npmjs.com/package/esbuild and confirm publisher/repo (github.com/evanw/esbuild), version ~0.28.1, and the documented postinstall behavior (platform-binary download).
    3. Decide the install path:
       - OPTION A (allow postinstall): permit esbuild's `node install.js` so `npx esbuild` runs natively; fastest build. Records a conscious reversal of the 01-01 least-privilege default for the build tool only.
       - OPTION B (least-privilege / tsc fallback): keep scripts deferred; do NOT run esbuild's postinstall. Plan 05's build uses `tsc --outDir dist` (no postinstall) instead of esbuild. Environment §Fallback confirms tsc covers all three entries.
  </how-to-verify>
  <resume-signal>Reply "approved: option-a" or "approved: option-b" (or describe concerns). Option chosen is recorded in the SUMMARY and drives Task 2 + Plan 05's build mechanism.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install the approved devDependencies and verify resolution</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (current devDependencies + type:commonjs + scripts — do NOT change test/typecheck here)
    - .planning/phases/04-electron-shell-secure-ipc-non-destructive-write/04-RESEARCH.md (§Standard Stack Installation; §Environment Availability)
  </read_first>
  <action>
    Add `electron@43.0.0` and `esbuild@^0.28.1` to `devDependencies` via the package manager, honoring
    the Task 1 decision. If OPTION A: run the normal install allowing the esbuild postinstall so the
    native binary is fetched. If OPTION B: install with scripts deferred / postinstall NOT run for
    esbuild (least-privilege), and record in the SUMMARY that Plan 05 must build via `tsc --outDir dist`
    rather than the esbuild script. Do NOT install zod, vite, electron-vite, react, react-dom, or
    electron-builder (all deferred per D-01 / RESEARCH). Do NOT touch the existing `test` or `typecheck`
    scripts, `type:commonjs`, or the `main` field — those change in Plan 05, not here.
  </action>
  <acceptance_criteria>
    - `node -e "require.resolve('electron')"` exits 0 (electron package present).
    - `npm ls electron esbuild` lists both at the expected versions with no unmet-peer errors.
    - Under OPTION A: `npx esbuild --version` prints a version. Under OPTION B: this is expected to be unavailable and the SUMMARY records the tsc-fallback for Plan 05.
    - `package.json` devDependencies contain electron and esbuild; no runtime (`dependencies`) entries were added.
    - Existing `test` and `typecheck` scripts, `main`, and `type` fields are byte-unchanged.
  </acceptance_criteria>
  <verify>
    <automated>node -e "require.resolve('electron')" && npm ls electron esbuild</automated>
  </verify>
  <done>Both devDependencies resolve per the chosen option; least-privilege decision recorded; no unrelated package.json fields changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local devDependencies | Third-party package code (and postinstall scripts) enters the project. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-SC | Tampering | esbuild/electron install (postinstall scripts) | high | mitigate | Blocking human checkpoint (Task 1) verifies legitimacy on npmjs.com and consciously decides the esbuild postinstall vs the least-privilege `tsc` fallback before any script runs. Both packages are SUS `too-new` false-positives with official repos + CLAUDE.md endorsement. |
</threat_model>

<verification>
- `node -e "require.resolve('electron')"` succeeds.
- `npm ls electron esbuild` shows both installed.
- The chosen install option (A allow-postinstall / B tsc-fallback) is recorded in the SUMMARY for Plan 05 to consume.
</verification>

<success_criteria>
electron@43.0.0 and esbuild@^0.28.1 are installed as devDependencies (or esbuild deferred with the tsc-fallback recorded), the least-privilege decision was made consciously by the developer, and no unrelated package.json fields were touched.
</success_criteria>

## Artifacts this phase produces

The full Phase 4 artifact inventory (created across Plans 04-01 … 04-05):

**New source (pure, tsx-testable — no electron import):**
- `src/ipc/session.ts` — `ActiveSession`, `SessionStore` (single active session; load/getModel)
- `src/ipc/ipc-guards.ts` — `IpcArgError`, `WireEdit`, `assertEditsPayload`, `toEdits`, `WireChangeRow`, `toWireReport`
- `src/ipc/write-service.ts` — `WriteInvariantError`, `defaultOutputPath`, `performWrite` (injected fs/dialog)

**New Electron wiring (thin; manual UAT only):**
- `electron/main.ts` — hardened `BrowserWindow`; `ipcMain.handle` for `save:load`/`save:getModel`/`save:preview`/`save:write`; native dialogs; `toErrorResult`
- `electron/preload.ts` — `contextBridge.exposeInMainWorld('saveEditor', { load, getModel, preview, write })`
- `electron/renderer.ts` — throwaway smoke-test host
- `electron/index.html` — host page + strict CSP meta

**IPC channels:** `save:load`, `save:getModel`, `save:preview`, `save:write`
**Bridge API:** `window.saveEditor.load()` / `.getModel()` / `.preview(edits)` / `.write(edits)`

**New tests:** `test/ipc/session.test.ts`, `test/ipc/ipc-guards.test.ts`, `test/ipc/wire-shape.test.ts`, `test/ipc/write-service.test.ts`

**Build / config:** `scripts/build.mjs` (esbuild 3 entries → `dist/`); `package.json` (`main` → `dist/main.js`, `build:electron` + `start` scripts, electron/esbuild devDeps); `tsconfig.json` (`include` += `electron/**/*.ts`)

**This plan (04-01) delivers:** the electron + esbuild devDependency install and the recorded least-privilege decision.

<output>
Create `.planning/phases/04-electron-shell-secure-ipc-non-destructive-write/04-01-SUMMARY.md` when done. Record the chosen install option (A allow-postinstall / B tsc-fallback) so Plan 05 selects its build mechanism accordingly.
</output>

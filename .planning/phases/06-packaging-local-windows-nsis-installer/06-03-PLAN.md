---
phase: 06-packaging-local-windows-nsis-installer
plan: 03
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified: []
autonomous: false
requirements: [PKG-01, PKG-02, PKG-03]
user_setup: []
must_haves:
  truths:
    - "npm run package on Windows produces release/MV2 Save Editor Setup 1.1.0.exe (PKG-01)"
    - "the installed app runs the full load -> browse/search -> edit -> preview -> write loop identically to the dev build (PKG-02)"
    - "install is per-user with no admin/UAC prompt, creates a Start Menu shortcut, and provides a working uninstaller (PKG-03)"
    - "the installed app shows its own build/icon.ico application icon (PKG-04 runtime)"
  artifacts: []
  key_links:
    - "the packaged app.asar resolves preload.js and index.html as siblings of dist/main.js (highest-risk link — RESEARCH Pitfall 2 / Phase 4)"
---

<objective>
The manual Windows acceptance gate: actually produce and install the `.exe`, then exercise the full editor loop on real Windows. This is the packaging analogue of v1.0's mandatory in-game load check — the runtime truths of PKG-01/02/03 cannot be asserted on the Linux dev host because electron-builder needs Wine (absent) to run rcedit + makensis (RESEARCH "Cross-Platform Reality"). The developer runs Windows per PROJECT.md, so this gate runs on their machine.

Purpose: Prove the config from Plan 06-02 + the icon from Plan 06-01 actually yield a working per-user installer whose installed app behaves identically to the dev build. The single highest-risk unknown is whether the packed `app.asar` still resolves `preload.js`/`index.html` as siblings of `main.js` (STATE Blockers/Concerns) — only an installed run confirms it.

Output: a recorded acceptance note in the SUMMARY (pass/fail with observations); no source files change.
</objective>

<execution_context>
@/home/user/mv2-save-editor/.claude/gsd-core/workflows/execute-plan.md
@/home/user/mv2-save-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-packaging-local-windows-nsis-installer/06-RESEARCH.md
@.planning/phases/06-packaging-local-windows-nsis-installer/06-CONTEXT.md

# The success bar (4 criteria) is in ROADMAP Phase 6; the automatable slice is already green from Plans 06-01/06-02.
</context>

## Artifacts this plan produces

| Artifact | Kind | Notes |
|----------|------|-------|
| (none) | verification-only | Records a Windows acceptance note in the SUMMARY; no code/config changes |

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Manual Windows build-and-run acceptance</name>
  <read_first>
    - @.planning/phases/06-packaging-local-windows-nsis-installer/06-RESEARCH.md — "Cross-Platform Reality" (why this is Windows-only) and "Validation Architecture" (this is the manual gate equivalent to v1.0's in-game load). Also "Open Questions" #3: node_modules may need `npm ci`, and the first electron-builder run downloads its own binaries — ensure network is available on the Windows machine.
    - @.planning/ROADMAP.md — Phase 6 "Success Criteria" (the 4 runtime truths this gate confirms).
  </read_first>
  <action>This is a blocking manual acceptance gate that runs on Windows (the Linux dev host cannot build the `.exe` — electron-builder needs absent Wine). Do NOT attempt to run `npm run package` on the Linux host. Guide the developer through the Windows build-and-run steps below, wait for their result, and record it in the SUMMARY. This gate is the packaging analogue of v1.0's mandatory in-game load and blocks phase completion until the developer reports the outcome.</action>
  <what-built>
    Plans 06-01 and 06-02 delivered (and automated tests already prove, cross-platform): the committed `build/icon.ico` (>=256x256), `electron-builder.json` (appId `com.jmptr.mv2-save-editor`, productName `MV2 Save Editor`, `directories.output: release`, lean `files`, nsis per-user flags), `package.json` at version `1.1.0` with the `package` script and the `electron-builder` devDependency. What remains is the Wine-blocked, Windows-only runtime proof: building the actual `.exe` and running the installed app.
  </what-built>
  <how-to-verify>
    Run these on a Windows machine (the developer's own box or, later, Phase 8 `windows-latest` CI):
    1. From a clean checkout, run `npm ci` (node_modules may be empty), then `npm run package`. Confirm it completes without touching esbuild's `dist/` and writes `release/MV2 Save Editor Setup 1.1.0.exe` (PKG-01). The first run downloads electron-builder's binaries — allow network.
    2. Double-click `release/MV2 Save Editor Setup 1.1.0.exe`. Confirm it installs per-user with NO admin/UAC prompt (`oneClick`/`perMachine:false`), and that a "MV2 Save Editor" Start Menu shortcut is created (PKG-03).
    3. Launch the installed app. Confirm the window/taskbar shows the app `.ico` (not the default Electron icon) (PKG-04 runtime).
    4. Run the FULL loop end-to-end, identical to the dev build (PKG-02): load a real `.sav`, browse/search a field, edit a value, preview the change, write to a NEW file. Then load that written file back in-game (or re-open it) to confirm it is not corrupt — this is the core-value check. A blank window or "index.html/preload not found" here means the asar broke sibling resolution (RESEARCH Pitfall 2) — report it.
    5. Uninstall via the generated uninstaller (Start Menu / Apps & features) and confirm it removes the per-user install cleanly (PKG-03).
    6. (Documented, not a defect) On first launch SmartScreen may warn "unknown publisher" — this is the accepted unsigned-build behavior (REQUIREMENTS Out-of-Scope), click through.
  </how-to-verify>
  <resume-signal>Type "approved" with the produced filename + a note that the full loop worked, or describe what failed (esp. any blank window / missing shortcut / UAC prompt).</resume-signal>
  <verify><human-check>On Windows: `npm run package` produces `release/MV2 Save Editor Setup 1.1.0.exe`; installing it is per-user (no UAC) with a Start Menu shortcut; the installed app runs the full load->browse->edit->preview->write loop and the written `.sav` is not corrupt; the uninstaller works.</human-check></verify>
  <done>Developer has run the Windows build + install + full-loop + uninstall check and recorded the pass/fail result (with the produced installer filename) in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unsigned installer -> end user Windows | The produced `.exe` is unsigned; Windows SmartScreen evaluates publisher reputation at install time |
| installed asar -> file system | The installed app reads/writes user-chosen `.sav` files (unchanged v1.0 trust boundary; non-destructive new-file write) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-02 | Spoofing (user trust) | unsigned Setup.exe (SmartScreen "unknown publisher") | medium | accept | Documented & accepted per REQUIREMENTS Out-of-Scope; code signing deferred to v2 (DIST-03). Click-through is expected, not a defect |
| T-06-05 | Denial of Service | installed app fails to launch (asar sibling resolution broken) | high | mitigate | Manual full-loop launch check (step 4) is the gate; a failure blocks the phase until `files: [dist/**]` layout is corrected in Plan 06-02 |
</threat_model>

<verification>
- Automated precondition (already green from Wave 1): `npm test` + `npm run typecheck` pass on any OS.
- Manual Windows gate: the developer confirms all six how-to-verify steps, recording the produced installer filename and the full-loop result in `06-03-SUMMARY.md`.
- The written `.sav` round-trips (loads without corruption) — the core-value acceptance, inherited from v1.0's mandatory in-game load.
</verification>

<success_criteria>
- `release/MV2 Save Editor Setup 1.1.0.exe` is produced by a single `npm run package` on Windows (PKG-01).
- The installed app completes the full load -> browse/search -> edit -> preview -> write loop identically to dev, and the output `.sav` is not corrupt (PKG-02).
- Install is per-user with no UAC, has a Start Menu shortcut and a working uninstaller (PKG-03), and shows the app icon (PKG-04 runtime).
- The acceptance result (pass or specific failure) is recorded in the SUMMARY as the phase's manual gate.
</success_criteria>

<output>
Create `.planning/phases/06-packaging-local-windows-nsis-installer/06-03-SUMMARY.md` when done.
</output>

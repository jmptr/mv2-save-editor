---
phase: 06-packaging-local-windows-nsis-installer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - electron-builder.json
  - package.json
  - package-lock.json
  - .gitignore
  - test/packaging.config.test.ts
  - test/packaging.dist-layout.test.ts
autonomous: false
requirements: [PKG-01, PKG-02, PKG-03, PKG-04]
user_setup: []
must_haves:
  truths:
    - "electron-builder.json sets appId com.jmptr.mv2-save-editor (D-02) and productName MV2 Save Editor (D-01)"
    - "directories.output is release, so electron-builder never writes into esbuild's dist/ (PKG-04)"
    - "nsis block sets oneClick true, perMachine false, createStartMenuShortcut true (PKG-03)"
    - "files includes dist/** and package.json so preload.js + index.html stay siblings of main.js inside app.asar (PKG-02)"
    - "package.json version is 1.1.0 (D-04) and lists electron-builder ^26 as a devDependency"
    - "npm run package chains build:electron && electron-builder --config electron-builder.json (D-05)"
    - "default artifactName resolves to MV2 Save Editor Setup 1.1.0.exe (PKG-01, no custom template)"
  artifacts:
    - electron-builder.json
    - package.json
    - .gitignore
    - test/packaging.config.test.ts
    - test/packaging.dist-layout.test.ts
  key_links:
    - "files: [dist/**, package.json] preserves path.join(__dirname, ...) sibling resolution after asar packing (Phase 4 Pitfall 2)"
    - "directories.output: release prevents collision with esbuild-owned dist/"
    - "win.icon: build/icon.ico must match the asset committed in Plan 06-01"
---

<objective>
Add the electron-builder packaging configuration and wire it into the project: a standalone `electron-builder.json` (chosen over `.yml` because JSON is `require()`-assertable in node:test with zero deps — RESEARCH Validation Architecture), the `package.json` edits (version 1.1.0, the `package` script, the electron-builder devDependency), the `.gitignore` entry for `release/`, and the cross-platform config + dist-layout assertion tests. This is the automatable core of PKG-01/02/03/04 — everything here is verifiable on the Linux host without Wine or electron-builder binaries.

Purpose: Encode every locked packaging decision (D-01..D-05) as declarative config, and pin it with tests so the config can never silently drift from the acceptance bar. The actual `.exe` production and installed-app behavior are deferred to the manual Windows gate (Plan 06-03) per the cross-platform reality in RESEARCH.

Output: `electron-builder.json`, edited `package.json`/`.gitignore`, and two new tests.
</objective>

<execution_context>
@/home/user/mv2-save-editor/.claude/gsd-core/workflows/execute-plan.md
@/home/user/mv2-save-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-packaging-local-windows-nsis-installer/06-RESEARCH.md
@.planning/phases/06-packaging-local-windows-nsis-installer/06-CONTEXT.md
@.planning/phases/06-packaging-local-windows-nsis-installer/06-PATTERNS.md

# The exact config key set is specified in 06-RESEARCH.md "Pattern 1" and 06-PATTERNS.md "electron-builder.json".
# The build this config wraps (dist/ sibling layout it must preserve):
@scripts/build.mjs
# node:test config-assertion pattern:
@test/scaffold.test.ts
</context>

## Artifacts this plan produces

| Artifact | Kind | Notes |
|----------|------|-------|
| `electron-builder.json` | NEW config | appId, productName, directories.output=release, files, win, nsis |
| `package.json` | EDIT | version 1.0.0 -> 1.1.0; +`package`/`package:dir` scripts; +`electron-builder` devDep |
| `package-lock.json` | EDIT | electron-builder + transitive deps pinned by the install |
| `.gitignore` | EDIT | add `release/` (bare `dist` ignore does not cover it) |
| `test/packaging.config.test.ts` | NEW test | asserts appId/productName/output/nsis flags/version/devDep/artifactName (PKG-01/03/04) |
| `test/packaging.dist-layout.test.ts` | NEW test | asserts dist/ siblings exist + files includes dist/** (PKG-01/02 automatable slice) |

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate — verify electron-builder before install</name>
  <read_first>
    - @.planning/phases/06-packaging-local-windows-nsis-installer/06-RESEARCH.md — "Package Legitimacy Audit": the legitimacy seam flagged `electron-builder` as SUS (`too-new`, `unknown-downloads`) because it could only see the latest release date and the downloads API was egress-blocked this session. This is a documented false positive — electron-builder is the canonical `electron-userland` packaging project. This checkpoint is the mandated pre-install confirmation for a [SUS] package and is NOT auto-approvable.
  </read_first>
  <action>STOP before any `npm install`. This is a blocking-human supply-chain gate for the [SUS]-flagged `electron-builder` (a documented false positive per RESEARCH). Present the verification steps below to the developer, wait for the explicit resume signal, and only then allow Task 2 to run `npm install -D electron-builder@^26`. Do NOT auto-approve — legitimacy checkpoints are never auto-advanceable.</action>
  <what-built>Nothing built yet. This gate precedes the first `npm install electron-builder`. The only new npm dependency this entire phase adds is `electron-builder` (the `.ico` is generated in-repo with zero deps in Plan 06-01).</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/electron-builder and confirm: publisher is the `electron-userland` project, repository is `github.com/electron-userland/electron-builder`, the `26.x` line is current (latest `26.15.x`), and weekly downloads are in the millions.
    2. Confirm the intended install target is `electron-builder@^26` (NOT `27.0.0-alpha` — pre-release, do not use).
    3. Confirm no other npm package is being added by this phase.
  </how-to-verify>
  <resume-signal>Type "approved" to authorize `npm install -D electron-builder@^26`, or describe concerns.</resume-signal>
  <verify><human-check>Developer confirms electron-builder's npmjs.com listing (electron-userland, 26.x current, millions of weekly downloads) and authorizes the install.</human-check></verify>
  <done>Developer has verified electron-builder legitimacy on npmjs.com and typed the resume signal authorizing `npm install -D electron-builder@^26`.</done>
</task>

<task type="auto">
  <name>Task 2: Create electron-builder.json, bump version, wire scripts, install, ignore release/</name>
  <files>electron-builder.json, package.json, package-lock.json, .gitignore</files>
  <read_first>
    - @.planning/phases/06-packaging-local-windows-nsis-installer/06-RESEARCH.md — "Pattern 1" and "Pattern 2" give the exact config keys and the chained `package` script.
    - @.planning/phases/06-packaging-local-windows-nsis-installer/06-PATTERNS.md — "electron-builder.json" and "package.json" sections list the locked keys and the self-edit deltas.
    - @package.json — current version `1.0.0`, existing chained-script convention (`"start": "npm run build:electron && electron ."`), devDependencies block.
    - @.gitignore — the bare `dist` ignore (line ~83) does NOT cover `release/`.
  </read_first>
  <action>Create `electron-builder.json` (JSON, not YAML — chosen for zero-dep require() assertability per RESEARCH Validation Architecture). Set exactly these keys: `appId` to `com.jmptr.mv2-save-editor` (D-02); `productName` to `MV2 Save Editor` (D-01); `directories.output` to `release` and `directories.buildResources` to `build` (PKG-04 — override the electron-builder default of `dist`, which esbuild owns); `files` to the two-entry array `dist/**` and `package.json` (lean asar — react/react-dom/@tanstack are already bundled into `dist/renderer.js`, so node_modules must NOT ship; RESEARCH Pitfall 5); `win.target` to `nsis` and `win.icon` to `build/icon.ico` (PKG-04, the asset from Plan 06-01); and an `nsis` block with `oneClick` true, `perMachine` false, `createStartMenuShortcut` true (PKG-03). Do NOT set a custom `artifactName` — the electron-builder default template already yields `MV2 Save Editor Setup 1.1.0.exe` (RESEARCH). Then edit `package.json`: change `version` from `1.0.0` to `1.1.0` (D-04); add a `package` script that chains `npm run build:electron && electron-builder --config electron-builder.json` (D-05), matching the existing `&&`-chained single-string script convention; optionally add a `package:dir` script chaining `npm run build:electron && electron-builder --dir --config electron-builder.json` for faster unpacked iteration and a Phase 8 CI-reusable bare form. Install the packager with `npm install -D electron-builder@^26` (pins the latest `26.x`, updates `package-lock.json`; do NOT accept `27.0.0-alpha`). Finally, edit `.gitignore` to add `release/` (RESEARCH Pitfall 1 — the bare `dist` ignore does not cover the new output dir). Do NOT modify `scripts/build.mjs` or `electron/main.ts` — electron-builder consumes `dist/` as-is.</action>
  <verify>
    <automated>node -e "const c=require('./electron-builder.json'); const p=require('./package.json'); if(c.appId!=='com.jmptr.mv2-save-editor'||c.productName!=='MV2 Save Editor'||c.directories.output!=='release'||!c.nsis.oneClick||c.nsis.perMachine!==false||!c.nsis.createStartMenuShortcut||p.version!=='1.1.0'||!p.scripts.package||!p.devDependencies['electron-builder']) process.exit(1); console.log('config wiring OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `electron-builder.json` parses and contains appId `com.jmptr.mv2-save-editor`, productName `MV2 Save Editor`, `directories.output` `release`, `files` including `dist/**`, `win.icon` `build/icon.ico`, and nsis `oneClick:true`/`perMachine:false`/`createStartMenuShortcut:true`.
    - `package.json` version is `1.1.0`, has a `package` script chaining `build:electron && electron-builder`, and lists `electron-builder` under devDependencies.
    - `.gitignore` includes a `release/` entry.
    - `scripts/build.mjs` and `electron/main.ts` are unchanged.
  </acceptance_criteria>
  <done>`electron-builder.json` encodes D-01..D-05, `package.json` is at 1.1.0 with the `package` script + electron-builder devDep, and `.gitignore` ignores `release/`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Config + dist-layout assertion tests</name>
  <files>test/packaging.config.test.ts, test/packaging.dist-layout.test.ts</files>
  <read_first>
    - @test/scaffold.test.ts — minimal node:test shape (`import test from 'node:test'`, `node:assert/strict`).
    - @.planning/phases/06-packaging-local-windows-nsis-installer/06-PATTERNS.md — "test/packaging.config.test.ts" and "test/packaging.dist-layout.test.ts" sections spell out the assertions.
    - @.planning/phases/06-packaging-local-windows-nsis-installer/06-RESEARCH.md — "Validation Architecture" Req->Test map ties each assertion to PKG-01/02/03/04.
  </read_first>
  <behavior>
    config test:
    - Loads `electron-builder.json` and `package.json` via `require()`.
    - Asserts appId === `com.jmptr.mv2-save-editor`, productName === `MV2 Save Editor`, directories.output === `release`.
    - Asserts nsis.oneClick === true, nsis.perMachine === false, nsis.createStartMenuShortcut === true.
    - Asserts files includes `dist/**`; asserts win.icon === `build/icon.ico`.
    - Asserts package.json version === `1.1.0`, devDependencies has an `electron-builder` entry, and scripts.package chains `build:electron` and `electron-builder`.
    - Derives the installer name from the default template and asserts `${productName} Setup ${version}.exe` === `MV2 Save Editor Setup 1.1.0.exe`.
    dist-layout test:
    - After `build:electron` has run, asserts `dist/main.js`, `dist/preload.js`, `dist/renderer.js`, `dist/index.html` all exist as siblings (existsSync).
    - Asserts electron-builder.json files includes `dist/**` (the glob that preserves the sibling layout inside app.asar).
  </behavior>
  <action>Create `test/packaging.config.test.ts` and `test/packaging.dist-layout.test.ts` using the node:test harness (NOT Vitest). Give each the repo's standard top-of-file block comment naming the PKG requirements it covers. In the config test, `require('../electron-builder.json')` and `require('../package.json')` and assert every locked value from the behavior block (covers PKG-01 filename derivation, PKG-03 nsis flags, PKG-04 appId/productName/output/icon). In the dist-layout test, use `existsSync` from `node:fs` to assert the four `dist/` files co-exist as siblings and assert the config `files` array includes `dist/**` (the cross-platform proxy for asar sibling resolution — PKG-01/02). Because the layout test depends on esbuild output, the plan's verify chains `build:electron` before `npm test`.</action>
  <verify>
    <automated>npm run build:electron && npm test && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `test/packaging.config.test.ts` and `test/packaging.dist-layout.test.ts` are picked up by the `test/**/*.test.ts` glob and PASS.
    - Config test proves appId/productName/output/nsis-flags/version/devDep and derives `MV2 Save Editor Setup 1.1.0.exe`.
    - Dist-layout test proves the four `dist/` siblings exist and `files` ships `dist/**`.
    - `npm run typecheck` is clean.
  </acceptance_criteria>
  <done>Two passing tests pin the config values (PKG-01/03/04) and the dist/ sibling layout + `files: [dist/**]` (PKG-01/02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> dev machine | Installing `electron-builder` (+ transitive `app-builder-bin`) pulls third-party code into the toolchain |
| build config -> packaged asar | `files` globs decide what source lands in `app.asar` shipped to end users |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-SC | Tampering | npm install of electron-builder | high | mitigate | Blocking-human legitimacy checkpoint (Task 1) before install; pin `electron-builder@^26` from `electron-userland`; commit `package-lock.json` |
| T-06-01 | Tampering / Elevation | electron-builder devDependency | high | mitigate | Exact `^26` pin (no `27-alpha`); lockfile committed; phase adds zero other npm deps |
| T-06-03 | Information Disclosure | over-broad asar leaking `src/`/node_modules | low | mitigate | `files: [dist/**, package.json]` ships only esbuild-bundled output — no `src/`, no node_modules |
| T-06-04 | Tampering | electron-builder downloads prebuilt binaries at build time | medium | mitigate | Lockfile + integrity; pre-caching `app-builder-bin`/NSIS resources is a Phase 8 CI concern (out of scope for the local config here) |
</threat_model>

<verification>
- `npm run build:electron && npm test && npm run typecheck` all green.
- Config wiring one-liner (Task 2 verify) exits 0.
- `git status` shows `release/` ignored (no accidental artifact tracking) and `dist/` still esbuild-owned.
- No changes to `scripts/build.mjs` or `electron/main.ts`.
</verification>

<success_criteria>
- Every locked decision D-01..D-05 is encoded in config and pinned by a passing test.
- The automatable slice of PKG-01/02/03/04 is green on the Linux host — no Wine, no electron-builder binaries required to verify.
- The config is ready for the manual Windows build-and-run acceptance in Plan 06-03.
</success_criteria>

<output>
Create `.planning/phases/06-packaging-local-windows-nsis-installer/06-02-SUMMARY.md` when done.
</output>

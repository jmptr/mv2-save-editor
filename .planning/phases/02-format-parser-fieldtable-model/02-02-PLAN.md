---
phase: 02-format-parser-fieldtable-model
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/structural-walk.ts
  - test/structural-walk.test.ts
  - test/helpers/malformed.ts
autonomous: true
requirements: [IO-01]
must_haves:
  truths:
    - "Reading the fixture yields version 20 and a SaveHeader summary: name 'Bob', gamemode 'Test', totalLevel 1366, header GP 953063625, header SlayerCoins 6511 (SC-1)"
    - "Header GP and SlayerCoins FieldEntries are marked readOnly + mirrors (never a write target) (SC-2)"
    - "The entity list declares 33 entities and the component walk lands delta-0 (walked==declared, cursor==regionEnd) on every one of the 33 entities (Determinism)"
    - "A malformed/oversized count or region size, or a size that overruns its enclosing region, throws a bounded typed error — no OOB read, no giant allocation (SC-5)"
    - "An unknown/newer save version warns-but-parses (sets unknownVersion) rather than hard-failing (SC-5)"
  artifacts:
    - src/structural-walk.ts
    - test/structural-walk.test.ts
    - test/helpers/malformed.ts
  key_links:
    - "walkComponents asserts dataStart+size <= regionEnd before trusting size, and cursor==regionEnd after the loop — the integrity spine everything else rides on"
    - "SaveHeader field offsets are derived from the BinaryReader cursor after each variable-length string (never hard-coded) so non-ASCII/longer names don't shift a wrong offset"
---

<objective>
Build the deterministic structural spine: `int32 version` → `SaveHeader` → entity list → per-entity component list, walked with the Phase 1 `BinaryReader` and emitted as `FieldEntry`s. This is the framed `[string name][int32 size]` walk the research verified lands delta-0 on all 33 entities. It records the version (+unknownVersion warn flag), the character summary, the header GP/SlayerCoins as read-only mirrors (SC-2), and every entity's `{id, start, size}` span (opaque, so Phase 4 can round-trip untouched entities byte-intact). It owns the SC-5 bounds guards and the Determinism integrity asserts. It exposes the located entity spans and component lists that Wave 3 wires to the region parsers.

Purpose: Delivers the IO-01 core — parse version → SaveHeader → entity list, offsets fresh from the cursor. The count-vs-region bounds checks here are the phase's primary security control (V5 Input Validation).

Output: `src/structural-walk.ts`, `test/structural-walk.test.ts`, `test/helpers/malformed.ts`.
</objective>

<execution_context>
@/home/jm/projects/mv2-save-editor/.claude/gsd-core/workflows/execute-plan.md
@/home/jm/projects/mv2-save-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-format-parser-fieldtable-model/02-RESEARCH.md
@docs/current-skill.md
@src/binary-reader.ts
@src/field-table.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: version + SaveHeader + entity/component walk with delta-0 integrity</name>
  <files>src/structural-walk.ts, test/structural-walk.test.ts</files>
  <read_first>02-RESEARCH.md §Pattern 1 + §Code Examples (walkComponents, verified fixture reference values table), docs/current-skill.md §File Layout + §SaveHeader field order, src/binary-reader.ts</read_first>
  <behavior>
    - `readVersion(reader)` reads int32 version at offset 0; returns { version, unknownVersion } where unknownVersion is true for version != 20 (the known fixture version) — warn-but-parse, never throw on version alone (SC-5).
    - `parseSaveHeader(reader)` walks UUID(16B) → CharacterName(string) → Gamemode(string) → timestamp → the active-entity/action strings → totalLevel(int32) → GP(int64) → SlayerCoins(int64) per docs/current-skill.md, deriving every post-string offset from `reader.offset` (never hard-coded). Emits FieldEntries; GP and SlayerCoins entries are `readOnly:true` with `mirrors` pointing at the wallet keys (SC-2). Fixture: name 'Bob', gamemode 'Test', totalLevel 1366, GP 953063625, SlayerCoins 6511; header ends at offset 150.
    - `walkEntities(reader)` reads int32 count (33), then N × [string id][int32 size][size bytes], recording each entity's {id, start, size} span; returns the spans. Entity list consumes exactly to byte 2,284,711 (before the 36-byte tail).
    - `walkComponents(reader, entityStart, entitySize)` reads int32 componentCount then count × [string name][int32 size][size bytes]; returns Component[] {name, dataStart, size}; asserts each `dataStart+size <= entityEnd` and that `cursor==entityEnd` after the loop (delta-0), throwing a typed ParseError otherwise.
    - Walk is generic (no hard-coded entity ID list — the fixture contains MelvorBase:Layout not in the docs list).
  </behavior>
  <action>Create src/structural-walk.ts with readVersion, parseSaveHeader, walkEntities, walkComponents as pure functions over BinaryReader (Phase 1) emitting FieldEntry (Plan 01). Read strings via reader.readString and take offsets from reader.offset — never hard-code post-string offsets (Pitfall 4). Mark header GP/SlayerCoins readOnly + mirrors (SC-2). Assert count==walked and cursor==regionEnd per entity region and throw a typed ParseError on mismatch (Determinism / fail-loud). Do NOT mutate buffer bytes; retain entity spans opaquely so Phase 4 round-trips untouched entities. Write test/structural-walk.test.ts FIRST (RED) using loadFixtureBuffer(): assert version 20 + unknownVersion false; summary name/gamemode/totalLevel/GP/SlayerCoins match the verified values; header GP/SlayerCoins entries are readOnly and carry mirrors; entity count 33; and that walkComponents lands delta-0 (count==walked, cursor==regionEnd) for ALL 33 entity regions.</action>
  <verify>
    <automated>npx tsx --test test/structural-walk.test.ts</automated>
  </verify>
  <done>Fixture parses to version 20 + summary matching the research reference table; header GP/SC are readOnly mirrors; all 33 entity regions walk delta-0; typed ParseError thrown on any region mismatch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SC-5 bounds guards + malformed-buffer helpers + version tolerance</name>
  <files>src/structural-walk.ts, test/structural-walk.test.ts, test/helpers/malformed.ts</files>
  <read_first>02-RESEARCH.md §Security Domain (threat patterns) + §Pitfall 3 (over-allocation), src/binary-reader.ts (native OOB + 7-bit-prefix throws)</read_first>
  <behavior>
    - A giant int32 entity/component count (e.g. 2^31) does not drive an unbounded loop/allocation: the loop is bounded so an iteration cannot read past regionEnd, and a count whose implied minimum span exceeds the region throws a bounded typed error before allocating.
    - A negative size or a size where `dataStart+size > regionEnd` throws a typed ParseError.
    - A malformed 7-bit length prefix (>5 bytes) or a declared string length past the buffer propagates the BinaryReader's RangeError (Phase 1 T-1-02) — not swallowed.
    - A truncated region (declared size runs past buffer end) throws, never OOB-reads.
    - An unknown/newer version (e.g. 21) still parses the (well-formed) rest and sets unknownVersion — warn-but-parse.
  </behavior>
  <action>Add explicit region-bound checks to structural-walk.ts (bound every count against its enclosing region before looping; assert dataStart+size<=regionEnd; reject negative sizes) and let BinaryReader OOB / 7-bit-prefix RangeErrors propagate. Create test/helpers/malformed.ts exporting buffer-crafting helpers: an oversized-count buffer, a size-overruns-region buffer, an oversized-7-bit-prefix buffer, and a truncated-region buffer (small hand-built buffers, not the full fixture). Extend test/structural-walk.test.ts (RED→GREEN): each malformed buffer throws a bounded typed error (assert the throw, and that no allocation/hang occurs), and a version-bumped copy of the fixture header parses with unknownVersion=true.</action>
  <verify>
    <automated>npx tsx --test test/structural-walk.test.ts</automated>
  </verify>
  <done>Every crafted malformed buffer throws a bounded typed error (no OOB, no giant alloc); unknown version warns-but-parses (unknownVersion=true, rest parses); BinaryReader RangeErrors propagate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.sav` file bytes → structural walk | Embedded int32 counts and region sizes drive loop bounds and allocations; all are untrusted and must be bounded against the enclosing region before use. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Denial of Service | Giant int32 entity/component count → unbounded loop/alloc | high | mitigate | Bound every count against regionEnd before looping; reject a count whose minimum span exceeds the region (Task 2) |
| T-02-02 | DoS / Tampering | Malformed 7-bit length prefix (>5 bytes) → wrapped/huge read | medium | mitigate | Let BinaryReader.readString RangeError (Phase 1 T-1-02) propagate; do not swallow |
| T-02-03 | Tampering | Region size overruns entity/buffer | high | mitigate | Explicit `dataStart+size > regionEnd` check → typed ParseError before read (Task 1/2) |
| T-02-05 | Tampering | Silent under-parse (wrong offset) → plausible-but-wrong model | high | mitigate | Integrity asserts count==walked & cursor==regionEnd for all 33 entities; fail loud (Task 1) |
| T-02-SC | Tampering | npm/pip/cargo installs | n/a | accept | No packages installed this phase (zero new deps) — legitimacy gate is a no-op |
</threat_model>

<verification>
- `npx tsx --test test/structural-walk.test.ts` green
- `npx tsc --noEmit` exits 0
- Wave-merge gate: `npx c8 --100 --include 'src/**' --exclude 'test/**' npm test`
</verification>

<success_criteria>
- SC-1 (version + summary), SC-2 (header mirrors read-only), SC-5 (bounds + version tolerance), and Determinism (delta-0 on all 33 entities) proven against the fixture and crafted malformed buffers.
</success_criteria>

<output>
Create `.planning/phases/02-format-parser-fieldtable-model/02-02-SUMMARY.md` when done
</output>

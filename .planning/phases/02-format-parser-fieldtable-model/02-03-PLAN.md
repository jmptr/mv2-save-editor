---
phase: 02-format-parser-fieldtable-model
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/wallet-parser.ts
  - src/experience-parser.ts
  - test/wallet-parser.test.ts
  - test/experience-parser.test.ts
autonomous: true
requirements: [IO-01]
must_haves:
  truths:
    - "The Wallet component is parsed as [int32 count][(int64 amount + string currencyID) x count]; GP is matched by the ID string 'MelvorBase:GoldPieces' and SlayerCoins by 'MelvorBase:SlayerCoins', never by order (SC-1)"
    - "Fixture Wallet resolves GP=953063625 at offset 20511 and SlayerCoins=6511 at offset 20573, with PrayerPoints sitting between them (order not assumed)"
    - "Wallet currency FieldEntries are marked authoritative (the real write target); a missing required GoldPieces throws a typed RequiredFieldMissingError (D-03 fail-loud) (SC-2)"
    - "An Experience component is parsed as [double XP][int32 LevelCap][int32 Level]; fixture Woodcutting = XP 7439645.2 / cap 120 / level 93 (SC-1)"
    - "Experience is context-validated: 1<=cap<=200, 1<=level<=cap, finite non-negative XP; LevelCap is recorded readOnly, XP and Level writable (SC-3)"
  artifacts:
    - src/wallet-parser.ts
    - src/experience-parser.ts
    - test/wallet-parser.test.ts
    - test/experience-parser.test.ts
  key_links:
    - "int64 currency amounts held as bigint (Phase 1 readInt64) — GP/SC exceed 2^53, Number would corrupt the write target"
    - "Experience triple records distinct offsets: xp(+0), levelCap(+8, readOnly), level(+12) so Phase 3 never patches the cap"
---

<objective>
Build the two structural sub-parsers that turn a located component region into `FieldEntry`s: the **Wallet** parser (currency-by-ID, authoritative) and the **Experience** parser (XP/Cap/Level triple, context-validated). Both are pure functions over a `BinaryReader` + a region start, so they unit-test independently against the research's verified fixture offsets and get wired by the Wave 3 orchestrator.

Purpose: Delivers the currency and skill halves of SC-1, the wallet-authoritative half of SC-2, and the Experience context-validation half of SC-3. Currency-by-ID (not by order) is the research's decisive correctness fix — PrayerPoints sits between GP and SlayerCoins in the fixture.

Output: `src/wallet-parser.ts`, `src/experience-parser.ts`, `test/wallet-parser.test.ts`, `test/experience-parser.test.ts`.
</objective>

<execution_context>
@/home/jm/projects/mv2-save-editor/.claude/gsd-core/workflows/execute-plan.md
@/home/jm/projects/mv2-save-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-format-parser-fieldtable-model/02-RESEARCH.md
@src/binary-reader.ts
@src/field-table.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wallet parser — currency-by-ID, authoritative, fail-loud on missing GP</name>
  <files>src/wallet-parser.ts, test/wallet-parser.test.ts</files>
  <read_first>02-RESEARCH.md §Pattern 2 + §Code Examples (parseWallet, verified offsets), 02-CONTEXT.md D-03 (fail-loud on missing required field), src/field-table.ts</read_first>
  <behavior>
    - `parseWallet(reader, dataStart)` reads int32 count, then count × [int64 amount (bigint) + string currencyID], recording each amount's offset.
    - Currencies are keyed by ID string: GP := 'MelvorBase:GoldPieces', SlayerCoins := 'MelvorBase:SlayerCoins' — matched by string, never by position (fixture order is GoldPieces, PrayerPoints, SlayerCoins).
    - GP and SlayerCoins FieldEntries are `authoritative:true` (the real write target).
    - GoldPieces is required: if absent from the Wallet, throw `RequiredFieldMissingError` with an actionable message ('no GoldPieces currency in Wallet') — never emit a silently-wrong model (D-03).
    - Fixture: GP=953063625n at offset 20511; SlayerCoins=6511n at offset 20573; Wallet dataStart=20507, count=3.
  </behavior>
  <action>Create src/wallet-parser.ts exporting parseWallet(reader, dataStart) that structurally walks the Wallet component and emits authoritative FieldEntries keyed by currency ID string, holding amounts as bigint (readInt64). Match GP/SlayerCoins by ID string, not order. Throw RequiredFieldMissingError (Plan 01) if GoldPieces is absent. Write test/wallet-parser.test.ts FIRST (RED) using loadFixtureBuffer() seeked to dataStart 20507: assert GP=953063625n@20511 and SlayerCoins=6511n@20573, that both are authoritative, that PrayerPoints is present between them (order not assumed), and that a crafted Wallet buffer lacking GoldPieces throws RequiredFieldMissingError with the actionable message.</action>
  <verify>
    <automated>npx tsx --test test/wallet-parser.test.ts</automated>
  </verify>
  <done>Wallet resolves GP/SlayerCoins by ID string with correct bigint values + offsets, both authoritative; missing-GP buffer fails loud with the actionable typed error.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Experience parser — XP/Cap/Level triple, context-validated, cap read-only</name>
  <files>src/experience-parser.ts, test/experience-parser.test.ts</files>
  <read_first>02-RESEARCH.md §Pattern (Experience) + §Pitfall 5 (writing LevelCap / wrong int) + §Code Examples (parseExperience, verified offsets), src/field-table.ts</read_first>
  <behavior>
    - `parseExperience(reader, dataStart, size)` reads [double XP][int32 LevelCap][int32 Level], recording xp(+0), levelCap(+8), level(+12) as distinct offsets.
    - Context-validation (SC-3): reject unless 1<=levelCap<=200, 1<=level<=levelCap, and XP finite & >= 0 — throw a typed ParseError on failure (fail-loud); reject a size < 16.
    - LevelCap FieldEntry is `readOnly:true`; XP and Level are writable (Phase 3 targets).
    - Fixture Woodcutting: dataStart=47405 → XP 7439645.2 / cap 120 / level 93.
  </behavior>
  <action>Create src/experience-parser.ts exporting parseExperience(reader, dataStart, size) that emits xp/levelCap/level FieldEntries at distinct offsets, with levelCap readOnly (Pitfall 5 — never let Phase 3 patch the cap). Context-validate cap/level/XP ranges and throw a typed ParseError (Plan 01) on any failure or on size<16. Write test/experience-parser.test.ts FIRST (RED) using loadFixtureBuffer() at Woodcutting dataStart 47405: assert XP 7439645.2, cap 120, level 93, distinct offsets +0/+8/+12, levelCap readOnly; and that crafted triples with level>cap, cap>200, negative/NaN XP, and size<16 each throw the typed ParseError.</action>
  <verify>
    <automated>npx tsx --test test/experience-parser.test.ts</automated>
  </verify>
  <done>Woodcutting Experience parses to 7439645.2/120/93 at distinct offsets with cap readOnly; every out-of-range/undersized triple throws the typed context-validation error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.sav` component region → Wallet/Experience parser | Currency count and Experience values are untrusted; counts bound the loop and values must pass context-validation before entering the model. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06 | Tampering | int64 currency parsed as Number → precision loss → wrong write target | high | mitigate | Amounts held as bigint (readInt64); currency matched by ID string not order |
| T-02-05 | Tampering | Wrong int in Experience triple (cap vs level) or invalid triple accepted | medium | mitigate | Distinct offsets recorded; context-validation (cap/level/XP ranges, size>=16) throws before model entry; cap readOnly |
| T-02-07 | Denial of Service | Missing required GoldPieces silently emitting a wrong model | medium | mitigate | RequiredFieldMissingError fail-loud on absent GP (D-03) |
| T-02-SC | Tampering | npm/pip/cargo installs | n/a | accept | No packages installed this phase (zero new deps) — legitimacy gate is a no-op |
</threat_model>

<verification>
- `npx tsx --test test/wallet-parser.test.ts` and `npx tsx --test test/experience-parser.test.ts` green
- `npx tsc --noEmit` exits 0
- Wave-merge gate: `npx c8 --100 --include 'src/**' --exclude 'test/**' npm test`
</verification>

<success_criteria>
- SC-1 (currency values + skill XP/Level/LevelCap), SC-2 (wallet authoritative), and SC-3 (Experience context-validation) proven against the fixture and crafted buffers.
</success_criteria>

<output>
Create `.planning/phases/02-format-parser-fieldtable-model/02-03-SUMMARY.md` when done
</output>

---
phase: 02-format-parser-fieldtable-model
plan: 05
type: execute
wave: 3
depends_on: [02-02, 02-03, 02-04]
files_modified:
  - src/save-parser.ts
  - test/save-parser.test.ts
autonomous: true
requirements: [IO-01]
must_haves:
  truths:
    - "parseSave(buffer) returns { fieldTable, viewModel }: it walks version -> SaveHeader -> entity list, locates the Bank entity's Wallet + Inventory components and every Experience-bearing entity, and wires the region parsers into one FieldTable (IO-01, SC-1)"
    - "The Bank wallet int64s are authoritative and the SaveHeader GP/SlayerCoins are read-only mirrors in both the FieldTable and the derived view model (SC-2)"
    - "The ViewModel is DERIVED from the FieldTable by projection and contains NO byte offsets — assertNoOffsets(viewModel) passes (SC-4)"
    - "Re-parsing the same buffer yields an identical FieldTable (identical offsets) — deterministic walk, offsets never persisted (SC-4)"
    - "An unknown/newer version parses with unknownVersion=true surfaced in the view model (SC-5)"
  artifacts:
    - src/save-parser.ts
    - test/save-parser.test.ts
  key_links:
    - "The orchestrator locates regions via walkEntities/walkComponents (Plan 02) then calls parseWallet/parseExperience/findStacks (Plans 03/04) — a skill is any entity carrying an Experience component"
    - "projectViewModel(fieldTable, entityIds) maps FieldEntries to the offset-free ViewModel; int64 values rendered as string"
---

<objective>
Wire the spine and the three region parsers into the phase deliverable: `parseSave(buffer) → { fieldTable, viewModel }`. It walks version → SaveHeader → entity list (Plan 02), finds the Bank entity's Wallet + Inventory components and every entity carrying an Experience component, calls the region parsers (Plans 03/04) to fill the FieldTable, then **derives** the offset-free ViewModel by projection. It proves the two cross-cutting success criteria that only exist at the whole-parse level: SC-4 determinism (re-parse → identical offsets) + no-offsets-in-view-model, and full-fixture SC-1/SC-2 integration.

Purpose: This is IO-01 delivered end-to-end — the single entry point Phase 3 (patcher, reads the FieldTable) and Phase 5 (UI, reads the ViewModel) consume.

Note (provisional decisions in play): the ViewModel content follows D-01 (lean + free metadata) and D-02 (raw IDs). 'Skill' = any entity with an Experience component (research A3) — Combat may surface as an XP-bearing entry; the projection may allow-list if the user prefers. Both are PROVISIONAL; flagged so the user can course-correct at review.

Output: `src/save-parser.ts`, `test/save-parser.test.ts`.
</objective>

<execution_context>
@/home/jm/projects/mv2-save-editor/.claude/gsd-core/workflows/execute-plan.md
@/home/jm/projects/mv2-save-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-format-parser-fieldtable-model/02-RESEARCH.md
@.planning/phases/02-format-parser-fieldtable-model/02-VALIDATION.md
@src/field-table.ts
@src/view-model.ts
@src/structural-walk.ts
@src/wallet-parser.ts
@src/experience-parser.ts
@src/inventory-parser.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: parseSave orchestration — wire spine + region parsers into one FieldTable (SC-1/SC-2)</name>
  <files>src/save-parser.ts, test/save-parser.test.ts</files>
  <read_first>02-RESEARCH.md §System Architecture Diagram + §Recommended Project Structure, all six Wave-1/2 src modules, 02-VALIDATION.md §Per-Task Verification Map</read_first>
  <behavior>
    - `parseSave(buffer)` returns `{ fieldTable, viewModel }`: reads version (+unknownVersion), parses SaveHeader (summary + header GP/SC mirrors), walks the 33 entities.
    - Locates the Bank entity (entity whose component list contains 'Wallet' and 'Inventory'), calls parseWallet on the Wallet component region and findStacks on the Inventory component byte region.
    - Treats every entity carrying an 'Experience' component as a skill and calls parseExperience on it.
    - Assembles one FieldTable: header GP/SC readOnly mirrors, wallet GP/SC authoritative, all 689 stacks, all skills' xp/levelCap(readOnly)/level.
    - Fixture integration: version 20, summary (Bob/Test/1366/953063625/6511), 689 stacks, Woodcutting xp 7439645.2/cap120/level93 present; wallet authoritative + header mirrors flagged.
  </behavior>
  <action>Create src/save-parser.ts exporting parseSave(buffer) that orchestrates readVersion + parseSaveHeader + walkEntities/walkComponents (Plan 02), then for the Bank entity calls parseWallet (Plan 03) and findStacks (Plan 04), and for every Experience-bearing entity calls parseExperience (Plan 03), collecting all FieldEntries into a single FieldTable. Locate the Bank by component names ('Wallet'+'Inventory'), not a hard-coded entity ID (fixture has MelvorBase:Layout not in docs). Write test/save-parser.test.ts FIRST (RED) using loadFixtureBuffer(): assert version 20, full summary, 689 stacks, Woodcutting triple present, header GP/SC readOnly+mirrors and wallet GP/SC authoritative in the FieldTable.</action>
  <verify>
    <automated>npx tsx --test test/save-parser.test.ts</automated>
  </verify>
  <done>parseSave(fixture) yields a FieldTable with correct version/summary/689 stacks/skills and correct authoritative-vs-mirror currency flags (SC-1, SC-2).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Offset-free ViewModel projection + SC-4 determinism + version tolerance</name>
  <files>src/save-parser.ts, test/save-parser.test.ts</files>
  <read_first>src/view-model.ts (offset-free type), test/helpers/no-offset-scan.ts (assertNoOffsets), 02-RESEARCH.md §Pattern 4 (derived view model), 02-CONTEXT.md D-01 (view-model scope)</read_first>
  <behavior>
    - `projectViewModel(fieldTable, entityIds)` derives the ViewModel from the FieldTable: version + unknownVersion, summary (int64 GP/SC rendered as string), bankItems (itemId, quantity, isPlaceholder, isLocked), skills (id, xp, level, levelCap), entityIds, and unresolvedFields (D-03 candidates) — with NO offsets.
    - `parseSave` returns this derived view model; `assertNoOffsets(viewModel)` passes (SC-4).
    - Determinism: `parseSave(buf).fieldTable` equals `parseSave(buf).fieldTable` — identical offsets on re-parse; offsets are re-derived each call, never cached/persisted (SC-4).
    - A version-bumped fixture buffer parses with viewModel.unknownVersion=true (SC-5), rest of the model intact.
  </behavior>
  <action>Add projectViewModel(fieldTable, entityIds) to src/save-parser.ts producing the offset-free ViewModel (Plan 01 type) with int64 as string, and have parseSave return it. Do not carry any offset into the view model (SC-4). Extend test/save-parser.test.ts (RED→GREEN): assertNoOffsets(viewModel) passes; two parseSave calls on the same buffer produce deep-equal FieldTables (identical offsets — determinism); GP/SC in the summary are strings; and a version-bumped copy of the fixture yields unknownVersion=true with the rest of the summary still correct.</action>
  <verify>
    <automated>npx tsx --test test/save-parser.test.ts</automated>
  </verify>
  <done>viewModel is offset-free (assertNoOffsets passes); re-parse yields identical offsets (deterministic); int64 rendered as string; unknown version warns-but-parses (SC-4, SC-5).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.sav` buffer → parseSave | The full untrusted buffer enters here; the orchestrator relies on the Wave 2 bounds/context-validation guards and must not persist or leak offsets across the FieldTable/ViewModel boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | Information Disclosure | Offsets leaking into the view model (breaks the SC-4 boundary) | medium | mitigate | ViewModel derived by projection with no offset field; assertNoOffsets asserted in Task 2 |
| T-02-09 | Tampering | Offsets cached/persisted across loads → stale wrong write target | high | mitigate | parseSave re-derives offsets every call; determinism test proves identical fresh offsets, none persisted |
| T-02-05 | Tampering | Bank located by hard-coded entity ID → wrong region on a variant save | medium | mitigate | Bank/skills located by component names, not hard-coded IDs (generic walk) |
| T-02-SC | Tampering | npm/pip/cargo installs | n/a | accept | No packages installed this phase (zero new deps) — legitimacy gate is a no-op |
</threat_model>

<verification>
- `npx tsx --test test/save-parser.test.ts` green
- `npx tsc --noEmit` exits 0
- Phase gate (Wave 3 merge): `npx c8 --100 --include 'src/**' --exclude 'test/**' npm test` — 100% on the parser core, full suite green before /gsd-verify-work
</verification>

<success_criteria>
- IO-01 delivered end-to-end via parseSave; SC-1 (full fixture parse), SC-2 (authoritative vs mirror in FieldTable + view model), SC-4 (offset-free derived view model + deterministic re-parse), SC-5 (version tolerance) all proven.
</success_criteria>

<output>
Create `.planning/phases/02-format-parser-fieldtable-model/02-05-SUMMARY.md` when done
</output>

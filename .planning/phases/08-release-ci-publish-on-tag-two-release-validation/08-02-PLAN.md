---
phase: 08-release-ci-publish-on-tag-two-release-validation
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - package.json
  - test/packaging.config.test.ts
autonomous: false
requirements: [CI-01, CI-02, CI-03]
user_setup: []

must_haves:
  truths:
    - "A real `v*` tag push runs the workflow on `windows-latest` and produces a DRAFT GitHub Release whose assets include the `.exe`, `latest.yml`, and `.exe.blockmap` (CI-01/02 runtime)"
    - "While the release is a draft, an installed client does NOT self-update; after a human clicks Publish, the feed resolves and the update is visible (CI-03 runtime)"
    - "`package.json.version` equals the pushed git tag — electron-builder derives the release from the version, not the git ref (Version=Tag invariant)"
    - "After bumping `package.json.version` to 1.1.1, `npm test` is green because `test/packaging.config.test.ts` version assertion was updated in the SAME change (lockstep)"
    - "An installed v1.1.0 client detects, downloads, and applies the published v1.1.1 update, and relaunches as v1.1.1 (SC4)"
  artifacts:
    - path: "package.json"
      provides: "version bumped 1.1.0 → 1.1.1 for the second release (Version=Tag invariant)"
      contains: "\"version\": \"1.1.1\""
    - path: "test/packaging.config.test.ts"
      provides: "version assertion updated to 1.1.1 in lockstep with the package.json bump"
      contains: "1.1.1"
    - "A recorded acceptance note in 08-02-SUMMARY.md (draft-release asset list, draft-invisibility check, manual-publish, and the two-release update proof)"
  prohibitions:
    - "Do NOT bump package.json.version without updating test/packaging.config.test.ts in the SAME commit (lockstep — line 39 asserts pkg.version; a lone bump breaks `npm test`)"
    - "Do NOT auto-publish the draft — CI-03 requires a human to click Publish (the safe gate)"
    - "Do NOT run `npm run package` / electron-builder on the Linux dev host (needs absent Wine — packaging runs on windows-latest CI, install/observe on the developer's Windows box)"
    - "Do NOT hand-create `v*` tags except for real releases (GSD `gsd/*` tags don't match `v*`, so no trigger collision)"
    - "Do NOT push tag `v1.1.1` while package.json still says 1.1.0 (Pitfall 1 — assets land on the wrong release; the updater never sees 1.1.1)"
  key_links:
    - from: "git tag vX.Y.Z"
      to: "package.json.version"
      via: "must be identical — electron-builder resolves the release for v${version}, not the git ref"
      pattern: "\"version\":\\s*\"1\\.1\\.1\""
    - from: "published release latest.yml"
      to: "installed client (electron-updater)"
      via: "the feed the updater reads on launch; drafts are invisible until published (CI-03 / SC4)"
      pattern: "latest\\.yml"
---

<objective>
The manual runtime acceptance gate for the phase: prove on real GitHub + real Windows the truths the
any-OS static test (Plan 08-01) cannot observe. Two milestones: (1) a real `v*` tag push produces a
DRAFT Release carrying `.exe` + `latest.yml` + `.blockmap`, the draft is invisible to the updater
until a human publishes it (CI-01/02/03 runtime); (2) the end-to-end two-release self-update proof
(SC4) — install v1.1.0, publish a v1.1.1 bump, and observe the installed v1.1.0 client detect →
download → apply the update and relaunch as v1.1.1.

Purpose: close the runtime half of CI-01/02/03 and deliver SC4, the whole reason auto-update lives in
Phase 8 (it is only fully observable across two sequential published releases). This mirrors the
Wave-2 manual Windows gates of Phases 6 (06-03) and 7 (07-03), which inherit the same Wine constraint:
the `.exe`/asar can only be produced/installed on Windows.

This plan also performs the single automatable code change the phase requires — the v1.1.1 version
bump — and encodes the Version=Tag invariant + the `test/packaging.config.test.ts` lockstep coupling
where that bump happens.

Output: the v1.1.1 bump (package.json + test in lockstep) and a recorded acceptance note in the
SUMMARY.
</objective>

## Assumptions (working defaults — no CONTEXT.md; confirm/override at execute)

`/gsd-discuss-phase` was skipped. Working defaults, confirm/override at execute:

- **SC4 is a MANUAL Windows acceptance gate** — it needs two real sequential published releases + a
  Windows client; it cannot run headlessly on Linux/CI. Mirrors the 06-03 / 07-03 gate shape.
- **Draft → manual publish** for BOTH v1.1.0 and v1.1.1 (CI-03). The developer clicks Publish in the
  GitHub Releases UI; nothing auto-publishes.
- **v1.1.0 needs no code change** — `package.json.version` is already `1.1.0` and the test already
  asserts `1.1.0`; the first release is just tag + push. Only the v1.1.1 bump (Task 2) edits files.

## Artifacts this phase produces

| Artifact | Kind | Notes |
|----------|------|-------|
| `package.json` (version 1.1.0 → 1.1.1) | config | Second-release bump; electron-builder derives the release from this, not the git ref |
| `test/packaging.config.test.ts` (assertion → 1.1.1) | test | Lockstep edit with the bump (line 39 asserts `pkg.version`) |
| `08-02-SUMMARY.md` acceptance note | docs | Draft-asset list, draft-invisibility, manual publish, and SC4 two-release update result |

<execution_context>
@/home/user/mv2-save-editor/.claude/gsd-core/workflows/execute-plan.md
@/home/user/mv2-save-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-release-ci-publish-on-tag-two-release-validation/08-RESEARCH.md
@.planning/phases/08-release-ci-publish-on-tag-two-release-validation/08-VALIDATION.md

# Manual-gate plan shape to mirror:
@.planning/phases/06-packaging-local-windows-nsis-installer/06-03-PLAN.md
@.planning/phases/07-auto-update-from-github-releases/07-03-PLAN.md

# The lockstep-coupled test:
@test/packaging.config.test.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: First release (v1.1.0) — tag push → draft Release with all 3 assets → draft-invisibility → manual publish → install (CI-01/02/03 runtime)</name>
  <files>(none — release procedure only; v1.1.0 is already the committed version. Produces GitHub Release assets: MV2 Save Editor Setup 1.1.0.exe, latest.yml, .exe.blockmap — not committed to the repo)</files>
  <read_first>
    - @.planning/phases/08-release-ci-publish-on-tag-two-release-validation/08-RESEARCH.md — § Two-Release Validation (SC4) Procedure (lines 362-385) and § Common Pitfalls 1-4 (version=tag, `contents: write`, `GH_TOKEN`, draft-feed).
    - @.planning/phases/07-auto-update-from-github-releases/07-03-PLAN.md — the manual-gate shape to mirror (blocking-human, Windows-only, recorded in SUMMARY).
    - @package.json — confirm `version` is `1.1.0` (no bump needed for the first release).
  </read_first>
  <action>
    Blocking manual acceptance gate. The developer drives real GitHub + Windows steps; guide them, wait
    for the result, and record it in the SUMMARY. Do NOT run `npm run package`/electron-builder on the
    Linux dev host (Wine absent). Steps:
    (1) Confirm `package.json.version` is `1.1.0`; ensure the tagged commit is pushed to `main` first;
        then `git tag v1.1.0` and `git push --follow-tags`. (GSD `gsd/*` tags do not match `v*`, so no
        trigger collision — Pitfall 6.)
    (2) Watch the `release` workflow run on `windows-latest` (Actions tab): `npm ci` → `build:electron`
        → electron-builder publish. Confirm it succeeds (no 403 → `contents: write` works; no auth skip
        → `GH_TOKEN` passed).
    (3) Inspect the created Release: it MUST be a DRAFT (CI-03) and its assets MUST include the `.exe`
        (`MV2 Save Editor Setup 1.1.0.exe`), `latest.yml`, and the `.exe.blockmap` (CI-02). Confirm the
        release is tagged `v1.1.0` (matches `package.json.version` — Version=Tag invariant / Pitfall 1).
    (4) Draft-invisibility check (CI-03): while it is still a draft, confirm an installed/packaged
        client does NOT see the update (updater.log records a feed miss, no update) — drafts are
        invisible to electron-updater by design (Pitfall 4).
    (5) Manually click "Publish release" on v1.1.0 in the GitHub Releases UI (the human gate).
    (6) Download and install the published `MV2 Save Editor Setup 1.1.0.exe` on Windows; launch once
        (SmartScreen "unknown publisher" click-through is the accepted unsigned behavior). Leave it
        installed for Task 2's two-release proof.
  </action>
  <what-built>
    Plan 08-01 delivered `.github/workflows/release.yml` (v* trigger, windows-latest, Node 22,
    build:electron before packaging, `contents: write`, `GH_TOKEN` env, `--publish onTagOrDraft`, default
    draft) and the static test that pins it. This gate proves the workflow actually runs on a real tag
    push and produces the correct draft Release + assets, and that the draft is invisible until a human
    publishes — the runtime facts a static test cannot observe.
  </what-built>
  <how-to-verify>
    On real GitHub + a Windows machine:
    1. `git tag v1.1.0 && git push --follow-tags` (after the commit is on the remote). The `release`
       workflow starts on `windows-latest`.
    2. The Actions run is green through the electron-builder publish step (no 403; no "token not found").
    3. The Release for `v1.1.0` is a DRAFT and its assets list contains: `MV2 Save Editor Setup 1.1.0.exe`,
       `latest.yml`, and `MV2 Save Editor Setup 1.1.0.exe.blockmap`.
    4. With the release still a draft, a packaged client does not update (updater.log shows the feed miss).
    5. Click Publish on v1.1.0; install the published `.exe`; launch it once. It runs the full editor loop.
  </how-to-verify>
  <resume-signal>Type "approved" with the draft's asset filenames (.exe + latest.yml + .blockmap) and a note that v1.1.0 was published + installed; or describe what failed (403 → check `permissions: contents: write`; assets on a `v1.1.0` release but tag pushed differently → Version=Tag mismatch; no draft → a forced-publish override slipped into the workflow).</resume-signal>
  <verify><human-check>On real GitHub + Windows: pushing `v1.1.0` runs the workflow on windows-latest and creates a DRAFT Release whose assets include the `.exe`, `latest.yml`, and `.exe.blockmap`; the draft is invisible to the updater; after manual Publish, the `.exe` installs and runs.</human-check></verify>
  <done>Developer has pushed `v1.1.0`, confirmed a green windows-latest run, a DRAFT Release with all 3 assets, draft-invisibility, then manually published and installed v1.1.0 — and recorded it (with the asset filenames) in the SUMMARY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Second release (v1.1.1) — lockstep version bump → tag push → draft → publish → observe two-release self-update (SC4)</name>
  <files>package.json, test/packaging.config.test.ts</files>
  <read_first>
    - @.planning/phases/08-release-ci-publish-on-tag-two-release-validation/08-RESEARCH.md — § Two-Release Validation (SC4) Procedure steps 4-6 (lines 376-385) and § Pattern 2 Version=Tag invariant.
    - @test/packaging.config.test.ts — line 39 asserts `pkg.version === '1.1.0'`; this MUST change to `'1.1.1'` in the same commit as the package.json bump (and check the derived-artifactName test at lines 56-61, which composes `${pkg.version}` — it follows automatically once the bump is in).
    - @package.json — the `version` field (line 3) to bump.
  </read_first>
  <action>
    Blocking manual acceptance gate for the end-to-end two-release proof. This task has an automatable
    code change (the v1.1.1 bump) plus a manual Windows observation. Steps:
    (1) LOCKSTEP BUMP (automatable): edit `package.json` `version` `1.1.0` → `1.1.1`, AND in the SAME
        change edit `test/packaging.config.test.ts` so its version assertion reads `1.1.1` (line ~39;
        the artifactName-derivation test at lines 56-61 uses `${pkg.version}` and updates automatically).
        Run `npm test` — it MUST be green (this proves the lockstep; a lone bump would fail line 39).
        Commit the bump + test edit together.
    (2) `git tag v1.1.1` and `git push --follow-tags` (package.json.version now equals the tag —
        Version=Tag invariant; never push `v1.1.1` while package.json still says 1.1.0 — Pitfall 1).
    (3) Watch the workflow build a DRAFT Release `v1.1.1` with `.exe` + `latest.yml` + `.blockmap`.
    (4) Manually Publish v1.1.1 (the CI-03 human gate).
    (5) SC4 OBSERVATION (manual, Windows): launch the installed **v1.1.0** client from Task 1.
        electron-updater reads the published `latest.yml`, sees `1.1.1 > 1.1.0`, background-downloads
        (using the `.blockmap` for a delta), fires the native "update ready" notification, and installs
        on quit. Relaunch → the app is v1.1.1. Confirm via updater.log + the app's version.
    Do NOT auto-publish; do NOT run electron-builder on Linux. Record the SC4 result in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `package.json.version` is `1.1.1` AND `test/packaging.config.test.ts` asserts `1.1.1`, committed together; `npm test` is green (lockstep proven).
    - Tag `v1.1.1` pushed; the workflow produced a DRAFT `v1.1.1` Release with `.exe` + `latest.yml` + `.blockmap`.
    - v1.1.1 published manually.
    - The installed v1.1.0 client detected, downloaded, and applied the v1.1.1 update; relaunch shows v1.1.1 (SC4).
  </acceptance_criteria>
  <what-built>
    Task 1 left a published + installed v1.1.0. This task ships a second published release (v1.1.1) via
    the same workflow and observes the installed v1.1.0 client self-update end-to-end — the two-release
    proof that could not exist until now (auto-update is only fully observable across two sequential
    published releases; that is why SC4 lives in Phase 8).
  </what-built>
  <how-to-verify>
    1. Bump `package.json` to 1.1.1 + update `test/packaging.config.test.ts` to `1.1.1` in one commit;
       `npm test` passes and `npm run typecheck` is clean.
    2. `git tag v1.1.1 && git push --follow-tags`; the workflow produces a DRAFT `v1.1.1` with all 3 assets.
    3. Publish v1.1.1 manually.
    4. Launch the installed v1.1.0: within a short time it downloads the update and shows the "update
       ready" notification; on quit it installs; relaunch reports version 1.1.1 (check the About/title
       or updater.log entry recording the applied update).
  </how-to-verify>
  <resume-signal>Type "approved" once the installed v1.1.0 client updated to v1.1.1 (note the updater.log line + the relaunched version); or describe what failed (updater saw nothing → confirm v1.1.1 was PUBLISHED not left as a draft, and latest.yml is on the published release; downloaded but did not apply → check install-on-quit).</resume-signal>
  <verify>
    <automated>npm test && npm run typecheck</automated>
    <human-check>After publishing v1.1.1, the installed v1.1.0 client detects → downloads → applies the update and relaunches as v1.1.1 (confirmed via updater.log and the app version).</human-check>
  </verify>
  <done>The v1.1.1 bump + lockstep test edit are committed with `npm test` green; a draft `v1.1.1` Release was built and manually published; the installed v1.1.0 client self-updated to v1.1.1 end-to-end (SC4), and the result is recorded in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Releases feed → installed client | The published `latest.yml` + `.exe` are downloaded and the installer is executed by electron-updater on the user's Windows machine |
| draft vs published release → updater | Only a PUBLISHED release is visible to electron-updater; the draft state is the human gate |
| unsigned installer → Windows user | SmartScreen presents an "unknown publisher" prompt for the unsigned `.exe` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-06 | Tampering | downloaded update installer binary | high | mitigate | electron-updater verifies the downloaded `.exe` sha512 against `latest.yml` before applying (built-in, from Phase 7); confirmed working by the SC4 two-release apply |
| T-08-04 | Tampering | unintended / premature publish | high | mitigate | Draft-then-manual-publish (CI-03): CI only creates a draft; a human explicitly clicks Publish for both v1.1.0 and v1.1.1 |
| T-08-08 | Tampering (integrity) | version/tag mismatch → assets on the wrong release | medium | mitigate | Version=Tag invariant enforced by the lockstep bump + `npm test` (line 39) and by pushing `v${version}`; RESEARCH Open Q1 optional guard step available |
| T-08-07 | Spoofing (user trust) | unsigned installer / SmartScreen | low | accept | Documented + accepted (REQUIREMENTS Out-of-Scope); code signing deferred to v2 (DIST-03); updater sha512 integrity is still enforced on the delta |
</threat_model>

<verification>
- Automated (Task 2 lockstep): `npm test` + `npm run typecheck` green after the v1.1.1 bump — proves package.json.version and the test assertion move together.
- Manual runtime (Task 1): a real `v1.1.0` push yields a green windows-latest run + a DRAFT Release with `.exe` + `latest.yml` + `.blockmap`; the draft is invisible to the updater until published.
- Manual runtime (Task 2 / SC4): after publishing v1.1.1, the installed v1.1.0 client detects, downloads, and applies the update and relaunches as v1.1.1.
- Results recorded in `08-02-SUMMARY.md` as the phase's manual gate (mirrors 06-03 / 07-03).
</verification>

<success_criteria>
- Pushing `v1.1.0` triggers the workflow on windows-latest and creates a DRAFT Release with the `.exe`, `latest.yml`, and `.blockmap`; the draft is invisible to the updater until manually published (CI-01/02/03 runtime).
- The v1.1.1 bump is applied in lockstep with `test/packaging.config.test.ts` and `npm test` stays green (Version=Tag invariant encoded).
- The installed v1.1.0 client self-updates end-to-end to the published v1.1.1 (SC4).
</success_criteria>

<output>
Create `.planning/phases/08-release-ci-publish-on-tag-two-release-validation/08-02-SUMMARY.md` when done, including the recorded acceptance note (draft asset filenames, draft-invisibility + manual-publish confirmation, and the SC4 two-release update result with a sample updater.log line).
</output>

---
phase: 01-binary-primitives-brotli-codec
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tsconfig.json
  - package.json
  - test/fixtures/test-fixture.sav
  - test/scaffold.test.ts
autonomous: false
requirements: [IO-03]
must_haves:
  truths:
    - "tsconfig.json enables strict + noUncheckedIndexedAccess: true + exactOptionalPropertyTypes + noImplicitOverride + target ES2022 + module commonjs + moduleResolution node + lib [ES2022] (D-05/D-06/D-08)"
    - "package.json keeps \"type\": \"commonjs\" (D-06) and declares typescript, tsx, c8, @types/node as devDependencies with scripts.test → tsx --test and scripts.typecheck → tsc --noEmit (D-01/D-07)"
    - "test/fixtures/test-fixture.sav exists (git-mv'd from docs/test-fixture.sav per D-10) and is committed to git"
    - "npx tsx --test test/scaffold.test.ts exits 0 (toolchain runs TS tests via node:test)"
    - "npx tsc --noEmit exits 0 against the strict config (D-08 byte-level safety net active)"
    - "decompressing the moved fixture yields a valid 2,284,747-byte buffer (D-10 move did not corrupt it)"
  artifacts:
    - path: "tsconfig.json"
      provides: "Full-strict TypeScript config with noUncheckedIndexedAccess (D-08)"
      contains: "noUncheckedIndexedAccess"
    - path: "package.json"
      provides: "CommonJS project with devDeps + test/typecheck scripts"
      contains: "\"type\": \"commonjs\""
    - path: "test/fixtures/test-fixture.sav"
      provides: "Real 152KB .NET-produced Melvor Idle 2 save (round-trip golden, D-10)"
    - path: "test/scaffold.test.ts"
      provides: "Trivial node:test case proving tsx --test runs"
      contains: "node:test"
  key_links:
    - from: "tsconfig.json"
      to: "package.json"
      via: "module: commonjs matches \"type\": \"commonjs\""
      pattern: "commonjs"
    - from: "package.json scripts.test"
      to: "tsx --test"
      via: "D-07 tsx runs node:test on .test.ts files"
      pattern: "tsx --test"
    - from: "test/fixtures/test-fixture.sav"
      to: "docs/test-fixture.sav"
      via: "D-10 git-mv (source removed from docs/)"
      pattern: "test-fixture.sav"
---

<objective>
Stand up the Phase 1 toolchain and fixture scaffolding before any source code: a full-strict tsconfig.json (D-05/D-08), package.json with locked devDependencies + scripts (D-01/D-06/D-07), the real .sav fixture committed to test/fixtures/ (D-10), and a trivial test proving the toolchain runs. A blocking human-verify checkpoint gates devDep install for the two SUS-flagged packages (tsx, @types/node) per the package-legitimacy protocol.

Purpose: Everything in Wave 1 (codec + primitives + tests) depends on this toolchain being green. Getting the strict tsconfig + fixture in place first means Wave 1 plans can immediately write code and run `npx tsx --test` + `npx tsc --noEmit` without re-deriving the scaffold. This plan implements decisions D-01, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11 and mitigates supply-chain threat T-1-SC.

Output: tsconfig.json, updated package.json (devDeps + scripts), test/fixtures/test-fixture.sav (committed), test/scaffold.test.ts, installed node_modules with typescript/tsx/c8/@types/node.
</objective>

<execution_context>
@.opencode/gsd-core/workflows/execute-plan.md
@.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-binary-primitives-brotli-codec/01-CONTEXT.md
@.planning/phases/01-binary-primitives-brotli-codec/01-RESEARCH.md
@.planning/phases/01-binary-primitives-brotli-codec/01-VALIDATION.md
@docs/current-skill.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write tsconfig.json + update package.json + git-mv fixture + scaffold test</name>
  <read_first>
    - .planning/phases/01-binary-primitives-brotli-codec/01-CONTEXT.md (D-05/D-06/D-07/D-08/D-10 locked decisions)
    - .planning/phases/01-binary-primitives-brotli-codec/01-RESEARCH.md (section: Recommended Project Structure; section: Standard Stack; section: Validation Architecture)
    - .planning/phases/01-binary-primitives-brotli-codec/01-VALIDATION.md (section: Wave 0 Requirements)
    - package.json (existing — confirm "type": "commonjs" + stub test script; do NOT overwrite, use Edit)
    - .gitignore (confirm node_modules/ and dist/ are ignored)
  </read_first>
  <files>tsconfig.json, package.json, test/fixtures/test-fixture.sav, test/scaffold.test.ts</files>
  <action>
    Per D-05/D-08: Write tsconfig.json with compilerOptions: strict: true, noUncheckedIndexedAccess: true (D-08 byte-level OOB safety net — do NOT relax), exactOptionalPropertyTypes: true, noImplicitOverride: true, target: "ES2022", module: "commonjs" (D-06), moduleResolution: "node", lib: ["ES2022"], esModuleInterop: true, skipLibCheck: true, resolveJsonModule: true. Set include: ["src/**/*.ts", "test/**/*.ts"] so tsc --noEmit typechecks both src and test files. Omit outDir (D-07 — no dist; emit is disabled via --noEmit at run time, not via tsconfig).

    Per D-01/D-06/D-07: Update package.json via Edit (NOT Write — it already exists). Keep "type": "commonjs" (D-06). Replace the stub "test" script with "scripts": { "test": "tsx --test test/**/*.test.ts", "typecheck": "tsc --noEmit" }. Add "devDependencies": { "typescript": "^6", "tsx": "^4", "c8": "^11", "@types/node": "^24" }. Do NOT run npm install yet — Task 3 does that after the legitimacy checkpoint.

    Per D-10/D-11: Move the fixture via `git mv docs/test-fixture.sav test/fixtures/test-fixture.sav` (create test/fixtures/ first if needed). The fixture is committed to git (152KB, no LFS needed per D-10). Verify `ls docs/test-fixture.sav` now fails (moved, not copied) and `ls test/fixtures/test-fixture.sav` succeeds. Tests decompress it at runtime (D-11) — no separate decompressed golden.

    Write test/scaffold.test.ts (per D-03 — test files live in a top-level test/ directory mirroring src/): import test from 'node:test'; import assert from 'node:assert/strict'; a single test('scaffold: tsx --test runs', () => assert.ok(true)). This file is owned by this plan only; Plan 02 and Plan 03 create their own test files (test/codec.test.ts and test/primitives.test.ts respectively, also under test/ per D-03) — do NOT create those here.

    Do NOT create src/ files, src/codec.ts, src/binary-reader.ts, src/binary-writer.ts, test/codec.test.ts, or test/primitives.test.ts — Wave 1 plans own those.
  </action>
  <verify>
    <automated>test -f tsconfig.json && test -f test/scaffold.test.ts && test -f test/fixtures/test-fixture.sav && ! -f docs/test-fixture.sav && node -e "require('node:zlib').brotliDecompressSync(require('node:fs').readFileSync('test/fixtures/test-fixture.sav'))" && echo "fixture-valid-brotli"</automated>
  </verify>
  <acceptance_criteria>
    - tsconfig.json contains the literal string "noUncheckedIndexedAccess": true
    - tsconfig.json contains "module": "commonjs"
    - package.json scripts.test is "tsx --test test/**/*.test.ts"
    - package.json scripts.typecheck is "tsc --noEmit"
    - package.json devDependencies lists typescript, tsx, c8, @types/node
    - docs/test-fixture.sav does NOT exist; test/fixtures/test-fixture.sav DOES exist
    - test/scaffold.test.ts imports from 'node:test' and 'node:assert/strict'
    - The moved fixture is a valid Brotli stream (decompresses without error — confirms D-10 move did not corrupt it)
  </acceptance_criteria>
  <done>Scaffolding files written and fixture moved; ready for the devDep legitimacy checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify tsx + @types/node legitimacy before install (SUS false-positive gate, T-1-SC)</name>
  <files>package.json</files>
  <read_first>
    - .planning/phases/01-binary-primitives-brotli-codec/01-RESEARCH.md (section: Package Legitimacy Audit — tsx + @types/node flagged SUS "too-new" as false positives)
  </read_first>
  <action>
    This is a blocking human-verify checkpoint gating the `npm install` in Task 3 (T-1-SC supply-chain mitigation). Task 1 declared the four devDeps (typescript [OK], tsx [SUS too-new], c8 [OK], @types/node [SUS too-new]) in package.json but did NOT install them. typescript and c8 are pre-approved (OK verdict); tsx and @types/node were flagged "too-new" (latest version published within ~2 days of the research session) — both are canonical, high-traffic, well-maintained packages (false positives), but the package-legitimacy protocol requires a human to confirm before install. The human runs the six `npm view` checks below and confirms they match the research before approving. Do NOT run `npm install` until the resume-signal is "approved".
  </action>
  <what-built>
    Task 1 wrote the tsconfig + package.json + fixture + scaffold test. The four devDeps are declared in package.json but NOT yet installed. This checkpoint is the supply-chain mitigation for threat T-1-SC before the install in Task 3.
  </what-built>
  <how-to-verify>
    Run these six checks in a terminal and confirm the output matches the research:
    1. `npm view tsx version` — should print 4.23.0 (or a close real semver)
    2. `npm view tsx repository.url` — should point to github.com/privatenumber/tsx
    3. `npm view tsx scripts.postinstall` — should be empty (no install-time script)
    4. `npm view @types/node version` — should print 26.1.0 (or close)
    5. `npm view @types/node repository.url` — should point to github.com/DefinitelyTyped/DefinitelyTyped
    6. `npm view @types/node scripts.postinstall` — should be empty

    If all six match expectations, the SUS flags are confirmed false positives and install is safe. If anything looks off (unexpected postinstall script, repo URL mismatch, version wildly different from research), STOP and flag the concern before installing.
  </how-to-verify>
  <verify>
    <human-check>Human runs the six `npm view` commands above and confirms versions + repo URLs + empty postinstall match the research; typescript and c8 are pre-approved (OK verdict). Approve only if all six match.</human-check>
  </verify>
  <acceptance_criteria>
    - npm view tsx version prints a real semver (~4.23.0) with repo github.com/privatenumber/tsx and empty postinstall
    - npm view @types/node version prints a real semver (~26.1.0) with repo DefinitelyTyped and empty postinstall
    - Human typed "approved" (or described a concern to halt)
  </acceptance_criteria>
  <done>Human confirmed tsx + @types/node legitimacy; resume-signal received; Task 3 may proceed with `npm install`.</done>
  <resume-signal>Type "approved" to proceed with `npm install --save-dev typescript@^6 tsx@^4 c8@^11 @types/node@^24`, or describe a concern to halt.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Install devDeps + verify toolchain (tsx --test + tsc --noEmit) + commit fixture</name>
  <read_first>
    - package.json (confirm devDependencies block + scripts from Task 1 are present)
    - tsconfig.json (confirm strict config from Task 1)
    - test/scaffold.test.ts (the trivial test the verify runs)
  </read_first>
  <files>package.json, package-lock.json</files>
  <action>
    Run `npm install --save-dev typescript@^6 tsx@^4 c8@^11 @types/node@^24` (per the Task 2 checkpoint approval). This creates node_modules/ and package-lock.json (both gitignored per existing .gitignore — confirm they are not staged).

    Verify the toolchain end-to-end:
    1. `npx tsx --test test/scaffold.test.ts` — must exit 0 and report 1 passing test. This proves tsx runs .test.ts via node:test (D-01/D-07).
    2. `npx tsc --noEmit` — must exit 0. This proves the strict tsconfig (D-05/D-08) compiles cleanly; test/scaffold.test.ts typechecks under strict + noUncheckedIndexedAccess.
    3. Sanity-check c8 runs (not a gate yet — no src/ files to cover): `npx c8 --100 --include 'src/**' tsx --test test/scaffold.test.ts`. Expect a non-zero exit about "no files covered" — that is fine and expected; the coverage gate only applies once Wave 1 creates src/codec.ts + src/binary-reader.ts + src/binary-writer.ts. Just confirm c8 itself executes without crashing (a "no files to cover" message is acceptable here).

    Commit the fixture move + scaffolding: stage tsconfig.json, package.json, package-lock.json (if not gitignored — confirm via `git check-ignore package-lock.json`), test/fixtures/test-fixture.sav, test/scaffold.test.ts. Confirm `git status` shows docs/test-fixture.sav as deleted + test/fixtures/test-fixture.sav as added (a rename). Use a commit message like `chore(01-01): scaffold tsconfig + devDeps + fixture`.
  </action>
  <verify>
    <automated>npx tsx --test test/scaffold.test.ts && npx tsc --noEmit && test -f test/fixtures/test-fixture.sav && ! -f docs/test-fixture.sav && test -d node_modules/tsx && test -d node_modules/typescript && test -d node_modules/c8 && test -d node_modules/@types/node</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx --test test/scaffold.test.ts` exits 0 and reports 1 passing test
    - `npx tsc --noEmit` exits 0 (strict + noUncheckedIndexedAccess active, no type errors)
    - `node -e "require('node:zlib').brotliDecompressSync(require('node:fs').readFileSync('test/fixtures/test-fixture.sav'))"` succeeds (fixture is a valid Brotli stream)
    - node_modules/ exists with tsx, typescript, c8, @types/node installed (test -d each)
    - git status shows the fixture as a rename (docs/test-fixture.sav → test/fixtures/test-fixture.sav) committed
  </acceptance_criteria>
  <done>Wave 0 complete: toolchain green (tsx --test + tsc --noEmit pass), fixture committed, devDeps installed. Wave 1 can now write src/ + test/ files against a working strict TS setup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → node_modules | Untrusted-origin devDependency packages cross here at install time (typescript, tsx, c8, @types/node) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-SC | Tampering (supply chain) | npm devDep install (tsx, @types/node flagged SUS too-new) | high | mitigate | Task 2 blocking human-verify checkpoint confirms npm registry metadata (version, repo URL, empty postinstall) matches research before `npm install` in Task 3. typescript + c8 pre-approved (OK verdict). Never auto-approvable per protocol. |
| T-1-03 | Denial of Service (decompression bomb) | codec decompress path | medium | mitigate (owned by Plan 02) | Plan 02 pins maxOutputLength cap (256 MiB) on brotliDecompressSync. Fixture validity confirmed here in Task 3. |
| T-1-05 | Tampering (corruption) | codec compress options | medium | mitigate (owned by Plan 02) | Plan 02 codec API defensively rejects the large-window Brotli param. |
| T-1-01 | Tampering (wrong-width / wrong-endian write) | binary-writer | medium | mitigate (owned by Plan 03) | Plan 03 BinaryWriter exposes LE-only per-width methods; SC-4 negative tests detect corruption. |
| T-1-02 | Denial of Service (7-bit length-prefix overflow) | binary-reader readString | medium | mitigate (owned by Plan 03) | Plan 03 rejects length-prefix encodings exceeding 5 bytes with RangeError (>2GiB cap). |
| T-1-04 | Tampering / Information Disclosure (buffer over-read OOB) | binary-reader | medium | mitigate (owned by Plan 03) | Plan 03 uses Buffer.readXxxLE (throws ERR_OUT_OF_RANGE) + noUncheckedIndexedAccess active here (D-08). |
</threat_model>

<verification>
- `npx tsx --test test/scaffold.test.ts` passes (toolchain runs)
- `npx tsc --noEmit` passes (strict config active, D-08 noUncheckedIndexedAccess enforced)
- Fixture is a valid Brotli stream (decompresses without error)
- Fixture moved from docs/ to test/fixtures/ and committed (D-10)
- devDeps installed and importable (node_modules/{tsx,typescript,c8,@types/node} exist)
- Human verified tsx + @types/node legitimacy before install (T-1-SC mitigated)
</verification>

<success_criteria>
- tsconfig.json is full-strict with noUncheckedIndexedAccess (D-08)
- package.json is CommonJS (D-06) with test/typecheck scripts (D-01/D-07) + 4 devDeps declared
- test/fixtures/test-fixture.sav committed (D-10, D-11)
- `npx tsx --test` and `npx tsc --noEmit` both exit 0
- Human verified tsx + @types/node legitimacy before install (T-1-SC mitigated)
</success_criteria>

<output>
Create `.planning/phases/01-binary-primitives-brotli-codec/01-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

Phase 1 produces the following symbols and files (full phase list; this plan owns the ones marked **[Plan 01]**):

**Source symbols:**
- `compress`, `decompress`, `roundTrip`, `CODEC_PARAMS`, `MAX_DECOMPRESSED_BYTES` — `src/codec.ts` (Plan 02)
- `BinaryReader` class + `readInt32` / `readInt64` / `readDouble` / `readBool` / `readString` — `src/binary-reader.ts` (Plan 03)
- `BinaryWriter` class + `writeInt32` / `writeInt64` / `writeDouble` / `writeBool` / `writeString` — `src/binary-writer.ts` (Plan 03)

**Files:**
- `tsconfig.json` — full-strict TS config (D-05/D-08) **[Plan 01]**
- `package.json` — CommonJS + devDeps + scripts (D-01/D-06/D-07) **[Plan 01]**
- `src/codec.ts` — Brotli codec + length invariant + bomb cap + large-window rejection (Plan 02)
- `src/binary-reader.ts` — LE reader primitives (Plan 03)
- `src/binary-writer.ts` — LE writer primitives (Plan 03)
- `test/codec.test.ts` — IO-03 SC-1/SC-2 + per-quality + bomb-cap + large-window tests (Plan 02)
- `test/primitives.test.ts` — edge matrix + negatives + fixture slices (Plan 03)
- `test/fixtures/test-fixture.sav` — real .NET save, committed (D-10) **[Plan 01]**
- `test/scaffold.test.ts` — toolchain smoke test **[Plan 01]**
